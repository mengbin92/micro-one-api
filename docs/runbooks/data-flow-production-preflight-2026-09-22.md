# 数据流下一阶段：生产前置准备（2026-09-22）

用户明确要求直接在生产准备，并确认渠道 1 的 `kimi-k3` 与 Kimi 订阅账号的 `k3` 是两个供应商提供的同一模型。本次完成来源核对、测试凭据、审计回读和监控依赖准备；未执行故障矩阵、生成请求、充值或计费扩围。

## 同一模型的两个来源

对外使用 `kimi-k3`，统一 registry ID 为 **143**；供应商来源及计价身份继续独立。

| 来源 | 来源 ID | 映射 ID | 实际上游模型 | 状态 | 来源优先级 |
|---|---|---|---|---|---|
| 普通渠道 | 1 | 124 | `Kimi-K3` | 启用 | 1 |
| Kimi 订阅账号 | 5 | 289 | `k3` | 启用 | 0 |

两条映射均属于既有配置，本次未重建或切换。两个供应商 base URL 不同。独立 registry 条目 `k3`（145）处于禁用状态，不影响 143 → 账号 5 → 上游 `k3` 的映射，不为此次准备启用重复入口。

订阅账号 5 为 `static_key`，本组合不能覆盖 OAuth 刷新恢复验收。计费归属必须继续使用 `source_kind`、对应来源 ID 和实际上游模型，不能因模型相同合并供应商成本。订阅投影的 `channel_id` 不是普通渠道身份。

## 已完成的前置检查

- 9 个服务均运行；业务镜像未更换，仅重建 monitor 容器两次以调整配置。
- billing/log/channel/identity 最新迁移分别为 106/107/100/098；对账历史 47 条，管理回读成功，超过 10 分钟的 stale reservation 为 0。
- 9 月 21 日三条测试根请求仍各能回读 1 个 attempt 和 2 条选路事件；无认证 401、缺参数 400、空结果 200 均符合预期。
- 沿用测试用户 **2** 的现有余额；新建令牌 **28**，名称 `df-preflight-20260922`，仅允许 `kimi-k3`，有限额度 **4096 tokens**，截至 **2026-09-23 09:20:53 CST** 有效。鉴权成功，`/v1/models` 只返回该模型；已用额度为 0，准备期间未发生成请求。
- 令牌通过受信 identity RPC 创建；该 RPC 不提供有限额度字段，故在首次鉴权前，仅对新建且未使用的令牌执行条件更新，设置有限额度。未调整任何已有令牌或用户余额。
- 凭据仅存生产主机 `/root/.local/state/micro-one-api/acceptance-20260922/kimi-k3-token.json`，目录权限 0700、文件权限 0600；未写入仓库。临时 RPC 二进制已删除。令牌过期后须重新创建，不能将其视为永久测试凭据。

## 监控依赖修复与主动探测限制

执行前 monitor 缺少 `CHANNEL_GRPC_ENDPOINT`，回落到容器自身 `127.0.0.1:9002`；最近 30 分钟仍有巡检错误，直接指标累计 549 次 ListChannels Unavailable。

在生产 Compose 的 monitor 服务内补充 `CHANNEL_GRPC_ENDPOINT=channel-service:9002`，保留原镜像并重建后，首次 ListChannels、RecordChannelHealth RPC 均成功，巡检轮次为 success。但上游 `/models` 与追加 `/v1/models` 两个路径均返回 **200/text/html**，不满足当前探测要求。巡检轮次成功仅证明枚举完成，不能证明上游探测成功。

为避免不兼容探测反复影响选路，生产 monitor 服务单独设置 `CHANNEL_HEALTH_CHECK_ENABLED=false`。连接地址修复保留，现有真实请求健康反馈继续运行；主动巡检标为 **HELD**，不得记录为全面通过。修复或配置兼容探测后，再移除此覆盖并验收。

本次探测留下两次 `invalid models response` 健康失败。以 01:19:27 UTC 已观察到的 healthy/0 基线为依据，在确认渠道、错误原因、时间窗、计数和未熔断状态均匹配后，条件撤回这两次降级。最终为 healthy、失败计数 0、未熔断；没有写入成功事件，没有修改成功时间或账务。

生产配置备份：

- 修改地址前：`/opt/micro-one-api/docker-compose/docker-compose.yml.bak.data-flow-20260922T011658Z`
- 暂停探测前：`/opt/micro-one-api/docker-compose/docker-compose.yml.bak.probe-hold-20260922T013021Z`

恢复配置时只核对并修改 monitor 对应字段，避免用整份旧 Compose 覆盖后续他人变更。

## 下一次执行的条件

| 项目 | 当前结论 |
|---|---|
| 同模型双供应商配置、限定测试身份、管理回读 | 已具备；执行前重新检查令牌有效期、剩余额度及用户最近 5 分钟无其他请求 |
| 请求发出前的候选切换 | 已有 9 月 21 日样本；后续如执行，限定 model 143 的映射范围，记录原值并恢复 |
| 请求中途跨来源 failover | 仍受 chat candidate-list 来源锁及凭据执行路径限制；本次准备没有改变代码 |
| 强制失败脚本 | 默认先禁用来源再要求 fallback 指标增长，触发方式与断言不匹配；不能直接用于宣布真正失败回退 PASS |
| F7–F18 故障恢复矩阵 | 未执行；本次生产前置准备不等同于全服务停机、数据故障或外部平台实验授权 |
| canonical 扩围 | 未执行；仍为 `charge + allowlist=5:k3`，cache creation 独立 charge；须按新的固定窗口与外部证据门禁办理 |

本次没有把“缺隔离环境”改写为“故障恢复已通过”。后续实验需要按实际动作、影响范围和恢复方案执行，不能将当前配置准备结果外推为验收结果。

## 证据

- [生产前置准备及各动作脱敏证据](evidence/data-flow-production-preflight-2026-09-22.json)：保存各观察时间、来源映射、镜像、迁移、测试身份、审计摘要、monitor 配置修复、上游探测、暂停和条件恢复结果。
- [准备前只读验收](evidence/data-flow-readonly-2026-09-22.txt)
- [准备前固定窗口覆盖检查](evidence/data-flow-coverage-2026-09-22.json)

顶层生产快照采集于 01:36:27 UTC；随后 01:50:53 UTC 的健康状态条件恢复单独保留，最终健康状态见 `final_channel_health`，不可混用两个时点。
