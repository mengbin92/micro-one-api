# micro-one-api v0.27 阶段路线图

> 制定日期：2026-09-01\
> 最后更新：2026-09-04\
> 代码基线：`develop@9100ca7`（v0.26.5 Responses 用量来源归因修复）\
> 最近发布：[v0.26.5](../releases/release-v0.26.5.md)（2026-09-02）\
> **状态：进行中，当前唯一执行入口**。`docs/TODO.md` 与 `docs/README.md`
> 的下一阶段入口均以本文为准。\
> 定位：v0.26.5 已发布；当前依次完成 executor、canonical usage 两条生产
> 灰度，再做历史账务审计和轻量部署文档；本阶段不展开 Relay 全量迁移或大仓重构。

## 0. 结论与执行顺序

v0.23–v0.26 已经连续交付 executor 首切片、Web 产品化、模型能力注册和五桶计费审计。
v0.26.5 已于 2026-09-02 发布，当前主要风险不再是功能或发布缺口，而是
两条生产灰度尚未形成同一条可验证闭环：

- v0.26.5 已固化 legacy Responses / OneAPI 的 canonical usage 来源归因修复；本次
  只发布 tag 和制品，不重启已运行等价修复的生产 relay，因此 observe 起点不变；
- v0.26.4 作为独立的控制台性能 PATCH 已发布，tag 指向 `52c294c`；它包含
  migration `089`，不包含 `relay-gateway` 运行时变更，不改变下述 executor
  失败结论或 canonical observe 起算点；
- v0.26.3 的 CI、Security、Compose E2E 和 Playwright 已通过；
  [Release run 33458907699](https://github.com/mengbin92/micro-one-api/actions/runs/33458907699)
  首次执行因 `relay-gateway` 的 GitHub Actions cache export 瞬时错误失败，重跑失败任务后
  已全部成功，[GitHub Release](https://github.com/mengbin92/micro-one-api/releases/tag/v0.26.3)
  于 2026-09-01 11:25:37 CST 发布，9 服务双架构镜像完整；
- executor 第五次正式观察窗口于 **2026-09-01 10:43:15 CST** 重建；2026-09-02 的固定
  `[24h]` 快照显示 Messages 流式 P95 相对 legacy 回归约 69.7%，已判定 **FAIL** 并于
  10:50:06 CST 关闭 executor、恢复全量 legacy；
- 2026-09-03 生产 `/v1/responses` 集中出现 502；已排除容器、channel、凭据、上游地址和
  `GLM-5.3` 模型本身不可用，定位为请求级上游 4xx 未进入 Responses→Chat fallback，随后
  被 relay 的默认状态映射改写为 502；原始 4xx 尚因终止分支缺日志而不可见；
- canonical usage Phase 1 / 2 代码、迁移 `085`–`088`、定价快照和 Admin 审计页均已
  实现，但生产 `observe → charge`、历史只读审计和 `PromptExclusive` 退出条件仍未闭环；
- GitHub 开放 Issue #6 的截图诉求已有实现，剩余是完整操作指引/视频；Issue #9 的
  “个人使用保持简单”已有 SQLite lite compose 基础，下一步应验证最短部署路径，
  不据此增加新的抽象层。

严格按以下顺序推进，禁止同时改变 executor 路径与计费模式：

1. **✅ 发布基线已收口到 v0.26.5**，Responses 归因修复已有可重现 tag 和制品；
2. **✅ Executor 已给出 FAIL 书面结论并回滚 legacy**，不降低 P95 准入线；
3. **✅ Responses 4xx 误报 502 已完成代码闭环**，原始状态观测、显式状态矩阵和
   fallback 回归已通过；生产部署与 canonical observe 重置另行执行；
4. **再执行 canonical usage 灰度**；若为上述修复部署新 relay 镜像，当前窗口作废并从部署后
   首条合格自然样本重新起算 48h，不把修复部署与 `charge` 同时切换；
5. **补只读历史审计与个人部署文档**，最后决定 v0.27.0 发布。

## 1. 执行状态总览

| 优先级 | 主题 | 当前状态 | 完成边界 |
|--------|------|----------|----------|
| P0 | 发布基线收口 | ✅ 已完成（2026-09-02） | v0.26.5 Responses 归因 PATCH 已发布 |
| P0 | Executor 7 天观察 | ✅ FAIL 已回滚（2026-09-02） | 24h P95 超线；flag=false，保持 legacy，旧路径未删除 |
| P0 | Responses 4xx 误报 502 | ✅ 代码已完成（2026-09-04；未部署） | relay 记录真实上游状态；显式映射 4xx；fallback 与脱敏回归通过 |
| P1 | Canonical usage 生产灰度 | 🟡 observe 进行中 | 原窗口 2026-09-02 11:12:00 CST 起算；若部署 P0 修复则重新起算 48h |
| P1 | 历史账务只读审计 | ⬜ 待 observe | 默认只出报告；无证据不冲正，原账本保持 append-only |
| P2 | 个人部署与文档收口 | ⬜ 待执行 | lite compose 空环境 smoke、最短操作指引和 Issue 状态闭环 |
| P3 | Executor 扩面 / 遗留移除 | ⬜ 触发式 | 仅达到 §8 触发条件后独立立项 |

## 2. P0 — v0.26.3 发布制品收口（已完成）

### 2.1 收口记录

首次 Release run 中，`relay-gateway` 已完成 `linux/amd64` / `linux/arm64` 构建并成功
推送 `v0.26.3`、`0.26`、`latest` 三个 manifest，随后在
`cache-to: type=gha,mode=max` 导出阶段报 `error writing layer blob: not_found`，导致
`Publish GitHub Release` 被跳过。该错误发生在镜像推送之后，未发现代码编译或测试失败。

2026-09-01 重跑失败任务后：

- `Build and Push relay-gateway` 与 `Publish GitHub Release` 均成功，workflow attempt 2
  最终为 success，无非成功 job；
- GitHub Release 正文与 `release-v0.26.3.md` 一致，状态为非 draft、非 prerelease；
- `v0.26.3` tag 与 `main` 均指向 `37639d614aef1f2854445cc2e2d041bfca4de918`，
  未移动或复用 v0.26.2 / v0.26.3 tag；
- 逐一只读检查 Docker Hub manifest：9 个服务的 `v0.26.3`、`0.26`、`latest`
  均包含 `linux/amd64` 和 `linux/arm64`，同一服务的三个 tag 指向同一最终 digest。

### 2.2 最终镜像 digest

| 服务 | `v0.26.3` / `0.26` / `latest` 最终 digest |
|------|---------------------------------------------|
| relay-gateway | `sha256:9fef2d86d56a028dd8b2e167d13af01badf4ad6959cf9639379190b7e07f42c1` |
| admin-api | `sha256:942b9b053e3adf479fc131cdf14379e5e9eb2b5a5c4fae4c5d8856ec01ad5bf3` |
| identity-service | `sha256:1f7f5f3bb8ee6505ee1d71a526d33f5d55856d4cbf825c6139a1c2d8d9d7be92` |
| channel-service | `sha256:82ffd2d258643019b5998bf35535360dff93a1bd50c6f948d24c9fda407ca916` |
| billing-service | `sha256:74db115b7419ebcdfe655581ceb30c3944bf1e533c75feb90140c045bce6a903` |
| config-service | `sha256:3c9c796b8af0ce8c5d2bfae981524ea35169b782f425a0827e340e2d3d198e4d` |
| log-service | `sha256:ce6966dc5dc01755d16da81fce4d6bc6eaa3c56c51412ff50933c5f287939079` |
| monitor-worker | `sha256:0fdfc1870dfb567175c7e0bc1a0ed1e52abd7114db6515669d4bacf8f430584e` |
| notify-worker | `sha256:f4f7e29762394c79dfff9d96d9ec27ef6b5963ad3508f86f32136b09d4fe3643` |

### 2.3 版本决策

- v0.26.3 制品收口当时仅重跑失败任务，没有为 cache 故障创建 patch；
- 后续 v0.26.4 是独立的控制台性能 PATCH，已于 2026-09-01 发布，与上述
  cache export 重跑决策无关；
- 瞬时 cache export 错误未稳定复现，不修改业务代码、不移动 tag；
- 后续 Release cache 容错属于独立 CI 加固，不影响本次制品完成判定。

验收标准：

- [x] Release workflow 最终为 success，GitHub Release 可见；
- [x] 9 服务 × 2 架构镜像可解析，版本 tag 与不可变 digest 有记录；
- [x] Release E2E、CI、Security 继续保持 success；
- [x] 没有移动既有 tag，也没有因瞬时 cache 故障制造空 patch。

## 3. P0 — Executor 第五次 7 天观察闭环

观察数据、固定 PromQL 和事件记录继续以
[`v0.23-executor-observation.md`](./v0.23-executor-observation.md) 为唯一事实源；
本文只管理阶段依赖和最终决策，不复制日检数据。

### 3.1 冻结边界

从 2026-09-01 10:43:15 CST 起保持以下内容不变：

- `relay-gateway` 容器镜像和运行实例；
- `RELAY_ORCHESTRATOR_ENABLED`、allowlist 内容及其 HMAC 密钥；
- 与 canary 直接相关的模型映射、adaptor 和 timeout 配置；
- Prometheus 抓取配置和记录规则。

只允许安全事件或生产事故打断窗口；一旦发生部署、进程重建、指标中断超过既定抓取周期
或 allowlist 变化，立即记中断原因并按观察手册重新建立起点，不拼接两段窗口。

### 3.2 日检与最终决策

每日使用固定 `[24h]` 查询留证，同时保留当前进程累计窗口。最终 PASS 必须同时满足：

- 实际承载 allowlisted 流量的 endpoint / stream 有 orchestrator 样本；
- 成功率不低于同口径 legacy，且没有新增稳定错误类型；
- P95 相对 legacy 回归不超过 20%；
- `quota_error=0`，无重复 Commit、错误 Release、截断流 Commit 或 reservation 卡单；
- failover exhausted 未显著放大，billing / usage log 的 endpoint、stream、渠道和订阅
  来源归因一致。

最终只允许以下两种结论：

- **PASS**：保留旧路径和默认关闭策略，另行审批把 production allowlist 扩到少量明确
  caller；每次只改 allowlist，不同时改代码或计费模式；
- **FAIL**：清空 allowlist 或关闭 flag，记录最小复现和归因，只修复触发失败的边界后
  重启观察；不为赶版本降低准入线。

验收标准：

- [x] 正式 24h 快照触发 FAIL，已执行回滚；
- [x] 观察手册写入最终时间范围、样本、指标、FAIL 与回滚验证；
- [x] `docs/design/v0.23-roadmap.md` 的未完成观察项已有明确归档结论；
- [x] 后续决策为“保持 legacy”，不是直接默认开启或删旧路径。

### 3.3 最终记录（2026-09-02）

- 决定性口径为 `/v1/messages / true` 固定 `[24h]`：orchestrator 约 98 个 success，
  legacy 约 176 个 success + 3 个 502；成功率未退化，但 P95 为 48.3s / 28.46s，
  相对回归约 69.7%。
- quota、ledger dedupe、billing/log 来源归因、failover、usage invariant 和协议错误均无异常；
  现有指标不含 model/source，无法在不猜测的前提下把 P95 差异进一步归因到 executor 开销
  或 cohort 构成。该归因缺口不改变回滚决定。
- 10:50:06 CST 关闭 `RELAY_ORCHESTRATOR_ENABLED` 并只重建 relay-gateway；新容器首批
  Responses 流量全部为 legacy，`reserve=commit=2`。allowlist 保留但不生效，旧 handler 保留。

## 4. P0 — Responses 上游 4xx 误报 502

### 4.1 生产诊断记录（2026-09-03）

- 生产所有容器保持运行，MySQL / Redis 健康；channel 1 在故障时段内健康探测成功、连续
  失败为 0。线上主机到 `https://gpunexus.chengfangtech.com/api/v1` 的 DNS、TLS 和 HTTP
  连通正常。
- 通过 channel-service 内部 gRPC 解密链路取得 relay 实际使用的凭据后，向同一上游发送
  `GLM-5.3`、`max_tokens=1` 的非流式最小请求，返回 HTTP 200 且模型回显为 `GLM-5.3`。
  因此排除数据库密文、凭据失效、模型 ID 大小写、基础地址和 channel 整体不可用。
- `GLM-5.3` 在 17:17 CST 前持续成功；17:45:28–17:47:30 CST 出现 30 条相同估算量
  （6,602）的 released reservation，对应 selection 结果均为 `client_error`。相邻时段
  `kimi-k3` 也出现 7 条 released reservation，而其他模型仍有成功流量，说明触发条件是
  特定 Responses 请求内容/上下文，不是单一模型故障。
- 正常兼容链路中，上游原生 `/responses` 返回 405，relay 会降级到 `/chat/completions` 并
  成功。故障请求没有产生 `responses to chat fallback failed` 记录，结合 `client_error` 可
  确认真正的上游响应是一个不在当前 fallback 白名单内的 4xx；现有终止分支没有记录原始
  `upstream_status`，因此不能把 413 / 415 / 422 / 403 中的任一状态写成既定事实。
- 客户端最终看到 502，是因为 `mapUpstreamError` 只显式保留 400、402、429、502、503、504，
  其他状态全部进入 `default → 502`。这是错误呈现和不可观测性的直接根因；上游 4xx 的原始
  请求级触发原因需在补日志后用同一失败请求复现确认。

### 4.2 修复边界

修复归属 `relay-gateway`，当前证据不支持修改 channel-service、模型注册、数据库记录或线上
凭据。最小改动范围：

1. 在 Responses 原生调用作 fallback 决策前，以及最终返回错误前，记录低基数
   `upstream_status`、endpoint、stream、execution path 和脱敏后的错误类别；禁止记录
   Authorization、请求正文或完整上游错误正文；
2. 把对外状态策略改成显式矩阵，不再用 `default` 吞掉所有未知 4xx。413 明确表示请求过大；
   415 / 422 根据兼容语义映射为客户端请求错误；上游 401 / 403 仍需与客户端鉴权失败区分，
   不直接把上游凭据状态伪装成客户端 Token 401；
3. 仅在证明确属“上游不支持 Responses、但可安全转为 Chat”时扩展 415 / 422 fallback；413
   不重试、不 fallback。保留现有 400 / 404 / 405 / 501 / 502 / 503 行为；
4. 为普通/流式 `/v1/responses` 覆盖 401、403、413、415、422，断言 fallback 决策、最终
   HTTP 状态、selection result、reservation release 和日志脱敏一致；再用生产等价失败形状
   做一次受控回归。

### 4.3 实现记录（2026-09-04）

- 对齐同级 `sub2api` 的确定性客户端错误边界：请求错误不再统一伪装成可重试 502，
  并保留经过脱敏的 OpenAI `type` / `code` / `param` / `message`；上游 401 / 403 仍显式
  映射为 502，避免与客户端 Relay Token 鉴权失败混淆。
- 状态矩阵新增 413→413、415 / 422→400；413 不进入 fallback，415 / 422 仅在
  Responses 请求能通过共享转换器生成合法 Chat Completions 请求时才尝试 fallback。
  原有 400 / 404 / 405 / 501 / 502 / 503 fallback 行为保持不变。
- Responses 原生请求在 fallback 决策前和最终错误返回前都会记录 `upstream_status`、
  endpoint、stream、execution path、phase 和低基数错误类别；日志不再携带请求头、
  请求正文或上游错误正文，fallback 成功/失败日志也只记录状态和类别。
- 普通与流式矩阵覆盖 401、403、413、415、422，并断言上游路径、最终 HTTP 状态、
  selection result、reservation commit/release 和日志脱敏。`go test ./internal/server
  ./internal/biz/...`、`go test -race ./internal/server/...`、`go vet ./internal/server
  ./internal/biz/...` 均通过。
- Compose 全栈镜像逐个构建成功。原 smoke 的固定宿主机 8080 被本地既有 `api` 进程占用，
  未中断该进程；使用临时 override 将 Relay 映射到 18080 后完成等价 E2E：MySQL / Redis
  健康、migration 成功、9 个服务运行、内部健康/鉴权检查通过、Relay health=200、
  未认证 models=401。临时容器、网络和卷均已清理。
- 本次未部署生产，因此没有改变当前 canonical observe 窗口；部署新 Relay 镜像时仍须按
  本节规则记录 digest、首条合格自然样本并重新起算 48h。

验收标准：

- [x] 同一失败请求能在服务端看见准确且脱敏的原始上游状态，客户端不再收到无依据的 502；
- [x] 最小 `GLM-5.3` 请求继续 200，既有 405→Chat fallback 不回归；
- [x] 413 不重试；415 / 422 只有在转换契约成立时才 fallback；上游 401 / 403 不混淆客户端鉴权；
- [x] `go test ./internal/server ./internal/biz/...`、相关 race 测试和 Compose E2E 通过；
- [x] 本次未部署 relay；后续部署时记录新镜像 digest 和首条合格自然样本，并重新起算 canonical 48h observe。

## 5. P1 — Canonical usage 生产灰度

详细语义、SQL 和回滚规则继续以
[`token-usage-billing-semantics-remediation-2026-08-31.md`](./token-usage-billing-semantics-remediation-2026-08-31.md)
为准。本阶段只执行已经设计并实现的灰度，不重新设计计费模型。

### 5.1 前置条件

- v0.26.5 已发布，v0.26.3 / v0.26.4 发布制品完整；
- executor 窗口已 PASS，或已明确回滚到 legacy；
- 生产实际镜像 digest、migration 状态和环境变量已有只读快照；
- billing / log / channel owner schema 已依次应用 `085`–`088`，且三方言 migration
  门禁保持通过；
- 所有 consumer 均支持 v1 双写字段后，才能开启 relay producer gate。

### 5.2 灰度步骤

1. `BILLING_CANONICAL_USAGE_MODE=observe`，producer gate 关闭，验证旧 producer / 新
   consumer 兼容；
2. 小流量开启 `RELAY_CANONICAL_USAGE_PRODUCER`，连续观察至少 48 小时；
3. 用固定 SQL 和指标确认 `ambiguous=0`、`cost_mismatch=0`、unexplained delta=0、
   source / upstream model 覆盖率 100%，且 v1 priced 行没有非法 canonical 静默 fallback；
4. 先对 Kimi / Z.AI / MiniMax 明确范围切 `charge`，一次只扩大一个来源；
5. 小范围 charge 稳定并完成供应商账单抽样对账后，再决定全量；
6. 任一异常先退回 `observe`，additive migration 和历史 canonical ledger 不回滚、不删列。

### 5.3 生产执行记录（2026-09-02）

- 只读快照确认 billing/log/channel owner schema 已应用 `085`–`088`；billing 容器未显式设置
  `BILLING_CANONICAL_USAGE_MODE`，代码安全默认值为 `observe`。生产 compose 在本路线图建立前
  已存在 `RELAY_CANONICAL_USAGE_PRODUCER=1`；该事实在 executor 窗口内没有被改动，后续只把
  executor 回滚完成后的数据计入本阶段正式 observe。
- executor 回滚后首批 2 个 `/v1/responses` ledger 均为 v1/verified，dedupe、五桶金额和
  pricing hash 正常，但 `source_kind` / `upstream_model_id` 为空，未达到 100% 来源覆盖率，
  因此没有把 10:50:06 CST 直接记为合格 observe 起点。
- 根因是 legacy Responses 的成功结算分支构造 usage envelope 后遗漏既有
  `applyChannelInputs`，stored-response 分支也有同样缺口；已补齐普通、流式、fallback 与
  previous-response 路径，并为 OneAPI 显式渠道补齐同一来源字段。`go test ./internal/server`
  和 `go test -race ./internal/server/...` 均通过。
- linux/amd64 relay 镜像 `sha256:576491d0256d...` 已于 **2026-09-02 11:05:58 CST** 部署；
  executor 保持关闭、canonical producer 保持开启，旧镜像保留为
  `docker-compose-relay-gateway:rollback-20260902-110556`。
- 首条合格自然样本于 **2026-09-02 11:12:00.225 CST** 写入：`/v1/responses / true`、
  `channel / glm-5.3`、v1/verified/openai_subset，source/upstream 与 pricing hash 完整，
  `cost_mismatch=0`、ambiguous=0、dedupe 唯一，billing/log 各 1 条且归因一致。正式 48h
  observe 从该时刻起算，若不中断，最早于 **2026-09-04 11:12:00.225 CST** 满窗；零样本的
  11:05:58–11:12:00 启动区间不计时。

**2026-09-03 11:12 固定 `[24h]` 日检**：

- 固定查询区间为 **2026-09-02 11:12:00.225–2026-09-03 11:12:00.225 CST**；生产
  MySQL 会话为 UTC，因此 SQL 使用 `2026-09-02 03:12:00.225–2026-09-03
  03:12:00.225`。窗口首尾自然 consume 分别为 11:12:00.225 和 11:11:45.365 CST，
  不是零样本窗口。
- 截止点 `relay-gateway` 仍运行 `sha256:576491d0256d...`，
  `RELAY_CANONICAL_USAGE_PRODUCER=1`、`RELAY_ORCHESTRATOR_ENABLED=false`；billing
  未显式设置 canonical mode，继续使用代码默认 `observe`。billing/log/channel owner
  schema 分别存在 `085`、`086`、`087`，billing 另有 `088`；截止点 Prometheus 所有
  micro-one-api target 均为 `up=1`。
- 窗口内共 **673** 条 consume，全部为 `usage_contract_version=1`、
  `usage_parse_status=verified`、`canonical_present=1`；其中 openai_subset 373 条、
  anthropic_exclusive 282 条、无 cache 且无需判定 subset/exclusive 的空 semantics 18 条。
  `ambiguous=0`，按“只有 cache usage 必须带明确 semantics”的契约复核后非法 v1 行为 0。
- 最终执行来源为 channel 539 条、subscription 134 条；`source_kind`、对应 source id 和
  `upstream_model_id` 缺失均为 0。673 条均有 `pricing_config_hash` 且全部能关联 `088`
  快照；ledger dedupe key 缺失为 0、重复 key group 为 0，673 条均有关联 dedupe claim。
- billing 与 log 表没有共同的一对一 request key，不能用 `reference_id=request_id` 直接
  join；改为对两表共同的 22 个 usage/source/latency 字段做多重集校验，双方均为 673 条，
  658 个字段组合的计数差异为 0。
- 截止点 `[24h]` 指标中 usage invariant mismatch、parse anomaly、billing ambiguous 和
  source isolation 均无样本；异步 billing queue 当前值和 `[24h]` 最大值均为 0。
  canonical/legacy 非零 shadow delta 共 12 条：K3 subscription Responses subset 8 条，
  canonical 合计比 legacy 低 4,316 quota；Kimi channel Anthropic exclusive 4 条，
  canonical 合计比 legacy 高 14,569 quota。两组均能由已冻结快照和明确 semantics 重建，
  净差为 +10,253 quota，未出现第三种 unexplained delta；Prometheus histogram `_sum`
  含负观察值，不用 `increase(_sum[24h])` 作为金额口径，精确金额以 ledger 重建为准。
- 设计文档原始 `cost_mismatch` SQL 返回 **104**，没有直接记为 0。归因后全部是
  流式 `step-explore`：它们来自 v0.23 executor 受控延迟 cohort 的 100 次正式请求，以及
  开启前后各 2 次验证，精确对应 104 条已留证测试流量；逐桶成本合计为 0，而结算路径按
  `actualCost <= 0` 应用最小扣费 1，故每条都是 `amount=1`、差额=1。保留全窗口原始结果
  以便审计，但从 canonical 自然生产 cohort 排除后 `cost_mismatch=0`；没有随机差额、拆分
  重复或悬空快照，不作为 canonical charge 阻塞项。
- 上游成本审计有 **153** 条 `unpriced`（`step-explore` 139、`step-3.7-flash` 14）；Step
  系列当前上游成本显式配置为 0，代码只有在计算结果 `upstreamCost > 0` 时才标记
  `priced`，因此这些 `unpriced` 是预期标签，不代表漏配，也不单独阻塞 canonical charge。
  当前审计状态无法区分“显式为 0”和“没有配置”；若后续要求 Step 系列供应商成本/毛利
  对账，应另行保留该区分。其余 520 条为 priced。billing 日志中的 673 条 pricing snapshot
  唯一键冲突均来自“hash 已存在即复用”的预期分支，真实 commit 失败、ambiguous、fatal
  均为 0；15:20 CST 基础设施重启时 billing/log/channel 各有 7 次 MySQL 尚未就绪的启动
  失败，恢复后未见运行期错误。
  relay 在 executor 成对 cohort 验证中于 16:39/16:41 重建，但 canonical producer、镜像和
  billing observe 均未改变，权威 DB 记录连续，故不重置 canonical observe 起点。
- 阶段结论：**`[24h]` 样本、契约、来源、快照、幂等及 billing/log 一致性通过，继续
  observe，不进入 charge**。09-04 11:12 满 48h 后重复固定查询；切 charge 前需对上述
  12 条预期差额在目标来源范围内完成供应商账单抽样，不能仅凭日期放行。v0.23 的 104 条
  已留证测试流量和 Step 系列的显式零上游成本均不作为本阶段阻塞项。

**2026-09-03 charge 前证据抽样与 48h 验收准备**：

- 生产实例只读复核确认 canonical producer 仍开启、executor 仍关闭，billing 继续使用
  安全默认 `observe`；所有服务与 MySQL / Redis 均保持运行，没有为本次核对改配置或重启。
- 在生产服务器端按冻结快照逐桶重建成本，只返回匿名化计数：自然流量非零 delta 仍精确
  分为 subscription Responses/openai_subset 负向 8 条与 channel
  Anthropic/anthropic_exclusive 正向 4 条，未出现第三种自然流量组合。另有
  `step-explore` 受控 cohort 的负向 104 条和正向 1 条，继续与自然流量隔离。
- 首个 24h 窗口的 673 条 verified v1 均有 pricing hash 且能解析快照；billing/log 以
  24 个共同 usage/source/latency 字段做多重集复核，双方各 673 条、差异组 0。上述结果
  证明内部 usage、来源、定价快照和结算结果可重建。2026-09-03 运营补充确认 K3/Kimi
  使用固定月费订阅，费用为 `199/月`；该费用不能逐笔映射 token 或请求，不能把内部快照
  当作供应商发票，也不能把月费机械摊成单请求成本。对应自然 delta 行当前均使用
  `cost_source=subscription`，但非零 `upstream_cost` 只是内部配置的成本模型。
- 据此把 K3/Kimi 的“供应商账单逐笔 token 抽样”书面判定为**不适用**：canonical 用户
  计费语义改由 verified 上游 usage、billing/log 双写和冻结用户售价快照验收，不再因固定
  月费没有逐笔账单而单独阻塞有限来源 charge。真实供应商毛利是另一口径；在套餐币种、
  套餐范围和是否存在超额计费留证前，不得用内部 `upstream_cost` 宣称已完成供应商毛利
  对账。固定月费证据规则见
  [`scripts/reconcile/README.md`](../../scripts/reconcile/README.md#固定月费订阅的供应商证据口径)。
- 新增只读固定窗口脚本
  [`scripts/reconcile/canonical_observe_48h.sql`](../../scripts/reconcile/canonical_observe_48h.sql)，
  UTC 窗口冻结为 `2026-09-02 03:12:00.225`（含）至
  `2026-09-04 03:12:00.225`（不含）；SQL 门禁与固定 PromQL、敏感数据不出库约束见
  [`scripts/reconcile/README.md`](../../scripts/reconcile/README.md#canonical-usage-固定-48h-验收)。
  窗口未满时脚本必须显示 `WAIT`；只有窗口满后 SQL 全 PASS、指标为 0，且供应商证据按
  实际计费合同完成“逐笔对账或固定月费不适用”书面判定，才允许提出有限来源 charge 变更。
- 2026-09-03 13:57 CST 在生产执行了一次窗口未满预演：脚本完整执行并按设计返回
  `WAIT`；累计 681 条 consume 的契约/语义非法、ambiguous、来源缺失、snapshot 缺失、
  dedupe/claim 异常、自然流量持久化成本 mismatch、未解释自然 delta 和 billing/log
  多重集差异均为 0。自然 subscription Responses/openai_subset 负向 delta 随新流量由 8
  增至 10 条，仍属于既定可解释组合。相同起点到预演时刻的 invariant mismatch、parse
  anomaly、billing ambiguous、source isolation 与异步 billing queue 最大值均为 0。
  另通过生产 Admin `/api/log/{id}` 匿名化抽查一笔最新 verified v1 ledger：五桶 token、
  五桶成本、来源、pricing hash/snapshot、倍率与 cache-creation mode 字段均完整，snapshot
  hash 与 ledger 引用一致；核对过程未输出用户、请求、渠道、订阅、token 或金额明细。

验收标准：

- [ ] 48 小时 observe 窗口满足设计文档 §12；
- [ ] 小范围 charge 无重复 ledger、余额偏差、来源缺失或价格快照解析失败；
- [ ] Admin 审计页能从一笔 consume 还原五桶、单价、倍率、mode 和 config hash；
- [ ] 回滚演练只切配置即可恢复 observe，未依赖 schema down migration；
- [ ] 全量 charge 是否进入本阶段有书面决策，不以日期替代指标。

## 6. P1 — 历史账务只读审计

历史 ledger 缺少足够协议证据，不能根据 `cache_read_tokens > prompt_tokens` 或
`total_tokens` 数值关系自动退款/追扣。本阶段只补安全的审计入口：

1. 在既有 `scripts/reconcile` 体系中增加只读报告，按 `verified / candidate / unknown`
   分类；
2. 报告列出原 ledger、source、upstream model、usage contract、parse status、五桶成本、
   pricing hash、候选差额和证据来源；
3. 默认不写数据库、不调用 billing RPC；输出确定性 CSV/JSON 供人工与供应商账单核对；
4. 只有 `verified` 且获得显式审批的记录，才在后续独立任务中生成幂等 reversal 草案；
5. 任何冲正都新增 reversal ledger，禁止原地修改 consume ledger；订阅吸收与钱包部分按
   原请求归属分别处理。

验收标准：

- [ ] 默认模式在生产只读账号下可运行，代码路径没有写操作；
- [ ] fixture 覆盖 OpenAI subset、Anthropic exclusive、ambiguous 和证据不足四类；
- [ ] 同一输入重复运行输出稳定，不产生 ledger 或外部副作用；
- [ ] 自动冲正不属于 v0.27.0 发布前置。

## 7. P2 — 个人部署与文档收口

这一项回应 [Issue #6](https://github.com/mengbin92/micro-one-api/issues/6) 和
[Issue #9](https://github.com/mengbin92/micro-one-api/issues/9)，目标是把已经存在的能力
讲清楚并验证，而不是再建一套“个人版”架构。

1. 在全新目录按 `.env.lite.example + docker-compose.lite.yml` 执行 SQLite 启动 smoke，
   覆盖 migrate、登录、创建渠道、创建 Token、`/v1/models` 和一次聊天请求；
2. 把上述路径压缩为 10 分钟 Quickstart，明确默认端口、最低资源、密钥保存、备份和升级边界；
3. 核对 README 截图与 v0.26 Web 一致，补充缺失的端到端操作演示链接；
4. 在 Issue #6 回填文档/截图/演示证据后再关闭；Issue #9 回填 lite compose 与简化原则，
   由维护者决定是否关闭，不把“参考其他产品”直接变成功能清单。

验收标准：

- [ ] 空环境 lite compose smoke 可重复通过；
- [ ] Quickstart 从 clone 到首个成功 Relay 请求不依赖隐藏步骤；
- [ ] 文档不包含真实凭证，截图不暴露 Token、上游 API Key 或生产数据；
- [ ] 开放 Issue 的完成/顺延决定有公开证据。

## 8. P3 — 触发式事项与明确非目标

| 事项 | 触发条件 |
|------|----------|
| Executor production allowlist 扩面 | 第五次 7 天窗口 PASS，且一次只增加有限明确 caller |
| Executor 默认开启 | production allowlist 再完成独立观察周期；不与 v0.27.0 自动绑定 |
| Responses WebSocket 迁移 | HTTP / SSE production 路径稳定，且 WS 流量与维护成本证明确有收益 |
| 删除 legacy handler | executor 默认开启跨至少两个版本，无回滚依赖和协议缺口 |
| 移除 `PromptExclusive` | 跨至少两个版本 `usage_contract_version=0` 新流量归零 |
| 自动生成或执行历史冲正 | verified 证据、人工审批、幂等 reversal 设计和 dry-run 全部具备 |
| Relay 目录迁移 / Kratos 大仓重构 | 遗留路径已删除，且有明确维护或交付收益 |
| Admin / identity 热点拆分 | 对应子域再次发生实质修改；一次只拆一个域 |

本阶段明确不做：

- 不同时切 executor 路径和 canonical charge；
- 不移动 v0.26.2 / v0.26.3 / v0.26.4 / v0.26.5 tag，不在生产服务器构建镜像；
- 不为发布 cache 瞬时故障修改业务代码；
- 不根据模型名或 total 数值关系批量改历史账本；
- 不迁移 Responses WebSocket，不删除 legacy handler，不默认开启 executor；
- 不启动 Relay 全目录搬迁、Kratos v3 或新的微服务拆分。

## 9. 建议时间盒与发布边界

以下日期是依赖成立时的最早时间，不是绕过指标的承诺日期：

| 时间盒 | 工作 | 退出条件 |
|--------|------|----------|
| 2026-09-01 | v0.26.3 Release 收口 | ✅ 全部制品和 GitHub Release 完整 |
| 2026-09-01 | v0.26.4 控制台性能 PATCH | ✅ tag、发布说明、migration 089 与 GitHub Release 已发布 |
| 2026-09-02 | v0.26.5 Responses 归因 PATCH | ✅ 修复、测试、tag 与发布制品已固化；生产不重启 |
| 2026-09-01–09-08 | Executor 第五次窗口 | 7×24h PASS，或 FAIL 已回滚 |
| 2026-09-02–09-04 | Canonical producer 48h observe | 原窗口 09-02 11:12:00.225 CST 起算；部署 P0 修复则按 §5 重新起算 |
| Observe 通过后 48–72h | 有限来源 charge | 账务、快照、来源和回滚验证通过 |
| 灰度结论后 | 历史只读审计 + lite docs | 报告和空环境 smoke 可复现 |

v0.27.0 的发布门槛：

- P0 两项均闭环；
- canonical observe 完成，charge 至少有明确的“进入 / 顺延 / 回滚”决定；
- 本阶段产生的代码通过 `make verify`，账务或 migration 变更追加相关 race、E2E 和
  三方言生命周期测试；
- release note、CHANGELOG、README 按仓库 release workflow 同步；
- tag 前先验证 Release workflow 的 cache 容错，避免重复 v0.26.2 的部分发布。
