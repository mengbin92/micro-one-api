# v0.23 executor 新旧路径 7 天观察手册

> 制定日期：2026-08-24  
> 状态：第五次正式窗口已 **FAIL 并回滚 legacy**。窗口自 **2026-09-01 10:43:15 CST**
> 运行至 2026-09-02 10:50:06 CST；固定 `[24h]` 日检发现 `/v1/messages / true`
> orchestrator P95 相对 legacy 回归约 69.7%，超过 20% 准入线。2026-09-02 10:50:06 CST
> 已设置 `RELAY_ORCHESTRATOR_ENABLED=false` 并仅重建 relay-gateway，allowlist 仍为 1 项、
> 旧路径继续保留。完整判定与回滚验证见 §4 末尾。

## 1. 观察范围与计时起点

同一组指标覆盖以下入口：

- `POST /v1/chat/completions`，流式与非流式；
- `POST /v1/responses`，流式与非流式；
- `POST /v1/messages`，流式与非流式；
- `/v1/responses` WebSocket 保留在 `legacy`，但记录为 `stream=true`，不计作 executor
  迁移命中。

观察窗口从下列条件全部满足后的第一条 orchestrator 指标样本开始，记录精确 CST 时间：

1. 新镜像已部署，`RELAY_ORCHESTRATOR_ENABLED=true`，SHA-256 allowlist 非空；
2. 至少一个真实内部 caller 使用 allowlisted token 调用当前主流 SSE 入口；
3. `micro_one_api_relay_executor_requests_total{execution_path="orchestrator"}` 增加；
4. 同 endpoint / stream 仍有 `legacy` 样本可作为对照。

只打开开关、只有 legacy 流量、Prometheus 未抓取或 orchestrator 样本为零，都不能开始计时。
窗口要求连续 7×24 小时；发布新 executor 代码、指标抓取中断超过一个抓取周期，或关闭 flag
后再开启，都要记录中断并重新确认起点。

## 2. 指标口径

| 指标 | 关键标签 | 用途 |
|------|----------|------|
| `micro_one_api_relay_executor_requests_total` | `endpoint,stream,execution_path,status,result` | 请求量、成功率、5xx、流终止和结算错误 |
| `micro_one_api_relay_executor_request_duration_seconds` | `endpoint,stream,execution_path` | 端到端 P95；SSE 包含完整连接生命期 |
| `micro_one_api_relay_executor_quota_outcome_total` | `endpoint,stream,execution_path,outcome` | Reserve / Commit / Release 生命周期 |
| `micro_one_api_relay_executor_failover_total` | `endpoint,stream,execution_path,result,reason` | 切换成功、耗尽和首个失败原因 |

`result` 的稳定值包括 `success`、`error`、`stream_error`、`quota_error`。流式 HTTP 已经写出
200 后仍可能发生协议截断或结算失败，因此不能只用 HTTP status 判断成功；7 天结论必须同时
检查 `result` 和 quota outcome。

## 3. 固定 PromQL

以下查询分别执行 `[24h]` 日检和 `[7d]` 总结；面板可把区间做成变量。

请求量与 terminal result：

```promql
sum by (endpoint, stream, execution_path, result) (
  increase(micro_one_api_relay_executor_requests_total[24h])
)
```

成功率：

```promql
sum by (endpoint, stream, execution_path) (
  increase(micro_one_api_relay_executor_requests_total{result="success"}[24h])
)
/
sum by (endpoint, stream, execution_path) (
  increase(micro_one_api_relay_executor_requests_total[24h])
)
```

5xx 比例：

```promql
sum by (endpoint, stream, execution_path) (
  increase(micro_one_api_relay_executor_requests_total{status=~"5.."}[24h])
)
/
sum by (endpoint, stream, execution_path) (
  increase(micro_one_api_relay_executor_requests_total[24h])
)
```

P95（SSE 表示完整流持续时间，不与非流式混合）：

```promql
histogram_quantile(
  0.95,
  sum by (le, endpoint, stream, execution_path) (
    rate(micro_one_api_relay_executor_request_duration_seconds_bucket[24h])
  )
)
```

quota outcome：

```promql
sum by (endpoint, stream, execution_path, outcome) (
  increase(micro_one_api_relay_executor_quota_outcome_total[24h])
)
```

failover 结果与原因：

```promql
sum by (endpoint, stream, execution_path, result, reason) (
  increase(micro_one_api_relay_executor_failover_total[24h])
)
```

## 4. 每日留证

每天固定时间记录一行；三个 endpoint 应按 `stream` 分组，不把流式和非流式聚合比较。

| 日期 / 窗口 | endpoint / stream | orchestrator 请求数 | legacy 请求数 | 新 / 旧成功率 | 新 / 旧 5xx | 新 / 旧 P95 | stream_error | quota_error | Commit / Release 异常 | failover switched / exhausted | 结论 |
|-------------|-------------------|----------------------|---------------|---------------|-------------|-------------|--------------|-------------|-------------------------|-------------------------------|------|
| 2026-08-24，17:30–22:30 CST（首个 5h） | /v1/messages / true | 16 | 1 | 100% / 100% | 0 / 0 | ≤60s / —（n=1） | 0 | 0 | 无（reserve=commit=16 / 1） | 0 / 0 | 起点确认，首日无异常 |
| 2026-08-25，08:30 CST（累计 15h） | /v1/messages / true | 38 | 1 | 100% / 100% | 0 / 0 | 64.5s / —（n=1） | 0 | 0 | 无（reserve=commit=38，release=0） | 0 / 0 | 无异常 |
| 2026-08-25，08:30 CST（累计 15h） | /v1/chat/completions / true | 43 | 0 | 100% / —（无 legacy 对照） | 0 / — | 26.4s / — | 0 | 0 | 无（reserve≈commit=43） | 0 / 0 | 新增 endpoint 命中，无异常 |
| 2026-08-25，08:30–11:46 CST | /v1/messages / true | — | — | — / — | **503×120 + 502×6（result=error）** / — | — | — | — | — | — | **窗口中断，见下方中断记录** |
| 2026-08-25，11:47–12:15 CST（新起点后 28min） | /v1/messages / true | 3 | 0 | 100% / — | 0 / — | —（样本不足） | 0 | 0 | 无（reserve=commit=3） | 0 / 0 | 新起点初步确认，legacy 对照未出现 |
| 2026-08-25，11:47–14:45 CST（新起点后 3h） | /v1/messages / true | 12 | 0 | 100% / — | 0 / — | 54s / — | 0 | 0 | 无（reserve=commit=12，release=0） | 0 / 0 | 503 未在新镜像复现；legacy 对照仍未出现 |
| 2026-08-25，11:47–16:02 CST（新起点后 4.3h） | /v1/messages / true | 720（success 716 + error 1 + stream_error 3） | 0 | 99.4% / — | 502×1 / — | 47.9s / — | 3 | 0 | reserve=725，commit=716，release=5，差额 4 待观察 | 0 / 0 | 流量显著上升；出现首批 stream_error×3 与 502×1（疑似上游），reserve−commit−release=4 需跟踪是否卡单 |
| 2026-08-25 11:47–2026-08-26 08:59 CST（新起点后 21h） | /v1/messages / true | 962（success 957 + error 1 + stream_error 4） | 0 | 99.5% / — | 502×1 / — | 41.3s / — | 4（+1） | 0 | reserve=965，commit=957，release=7，差额收敛到 1（在飞连接，非卡单） | 0 / 0 | 卡单疑虑解除；stream_error 缓慢累积需继续跟踪 |
| 2026-08-25 11:47–2026-08-26 08:59 CST（新起点后 21h） | /v1/responses / true | 0（orchestrator 无样本） | 269（success 266 + error 3） | — / 98.9% | — / 502×2 | — / 37.7s | — | — | legacy 侧 reserve=commit=266 | — | responses 流量全部在 legacy（疑为 WebSocket，按 §1 不计迁移命中）；若其有 allowlisted 流量，7 天结论需 orchestrator 样本 |
| 2026-08-26，11:17–11:53 CST（二次部署后 36min） | /v1/messages / true | 21 | 0 | 100% / — | 0 / — | 41.1s / — | 0 | 0 | 无（reserve=22，commit=21，差额 1 在飞） | 0 / 0 | 二次部署后正常 |
| 2026-08-26，11:17–11:53 CST（二次部署后 36min） | /v1/responses / true | 40 | 43（success 42 + error 1） | 100% / 97.7% | 0 / 500×1 | 56s / 35.6s（+57%，样本小） | 0 | 0 | 无（reserve=41，commit=40） | 0 / 0 | **responses 首次走 orchestrator**；P95 早期偏高，继续观察是否超 20% 回归线 |
| 2026-08-26，11:17–11:53 CST（二次部署后 36min） | /v1/chat/completions / true | 1 | 0 | 100% / — | 0 / — | 28.5s / — | 0 | 0 | 无（reserve=commit=1） | 0 / 0 | 恢复命中，样本极少 |
| 2026-08-26 11:55–2026-08-27 11:55 CST（固定 `[24h]`） | /v1/messages / true | 1,357（success 1,341 + error 4 + stream_error 12） | 0 | 98.82% / — | 502×4（0.295%） / — | 59.5s / — | 12 | 0 | 无（reserve≈1,360，commit≈1,341，release≈18，差额≈1） | 0 / 0 | 无 legacy 对照；502 与 stream_error 需继续归因 |
| 2026-08-26 11:55–2026-08-27 11:55 CST（固定 `[24h]`） | /v1/messages / false | 145（success 142 + error 3） | 0 | 97.93% / — | 502×3（2.07%） / — | 30.0s / — | — | 0 | 无（reserve≈146，commit≈142，release≈3，差额≈1） | 0 / 0 | 首次留存非流式日检样本；无 legacy 对照 |
| 2026-08-26 11:55–2026-08-27 11:55 CST（固定 `[24h]`） | /v1/responses / true | 140（success 87 + error 53，其中 502×52、400×1） | 226（success 226） | **62.14% / 100%** | **37.15% / 0** | **300s / 54.6s（+449%）** | 0 | 0 | 无（reserve≈154，commit≈87，release≈66，差额≈1） | 0 / 0 | **触发 §5 回滚线：成功率、5xx、P95 三项不合格** |
| 2026-08-26 11:55–2026-08-27 11:55 CST（固定 `[24h]`） | /v1/chat/completions / true | 0 | 0 | — / — | — / — | — / — | — | 0 | 无样本 | 0 / 0 | 24h 窗口零样本；仅部署后首 36min 有 1 个 success，不能判定通过 |
| 2026-08-27 15:40–2026-08-28 09:48 CST（修复容器累计） | /v1/responses / true | 364（success 358 + error 6；502×5、500×1） | 0（重启后无新样本；固定 `[24h]` 另含重启前 success≈33、429×1） | 98.35% / — | 1.65% / — | 52.9s / — | 0 | 0 | 09:48 时 reserve=364、commit=358、release=5、差额 1；09:50 复核已闭合为 367=362+5 | 0 / 0 | StepFun Responses 已有 151 个成功 usage 样本；旧 25–70ms 本地转换 502 未复现。正式观察起点尚未成立 |
| 2026-08-27 15:40–2026-08-28 09:48 CST（修复容器累计） | /v1/messages / true | 144 个终态（success 142 + error 1/502 + stream_error 1）及 1 个在飞 | 0（重启后无新样本；固定 `[24h]` 另含重启前 success≈218、502×3、429×1） | 98.61% / — | 0.69% / — | 58.2s / — | 1 | 0 | 09:50 复核 reserve=153=commit 151+release 2 | 0 / 0 | 普通 error 为 StepFun 29.8s 渠道路由失败；无本地 adaptor 错误模式 |
| 2026-08-27 15:40–2026-08-28 09:48 CST（修复容器累计） | /v1/messages / false | 2（success 2） | 0（重启后无新样本；固定 `[24h]` 另含重启前 success≈13） | 100% / — | 0 / — | 29.0s / — | — | 0 | reserve=commit=2 | 0 / 0 | 样本极少，无异常 |
| 2026-08-27 15:40–2026-08-28 09:48 CST（修复容器累计） | /v1/chat/completions / true | 0 | 0 | — / — | — / — | — / — | — | 0 | 无样本 | 0 / 0 | 修复重启后仍无 canary 样本，不能判定通过 |
| 2026-08-28 10:33–10:41 CST（第四次窗口起点） | /v1/responses / true | 39（success 39） | 1（success 1，受控 `step-explore`） | 100% / 100% | 0 / 0 | 20.3s / —（legacy n=1，单次约 2.4s） | 0 | 0 | orchestrator reserve=40、commit=39、差额 1 在飞；legacy reserve=commit=1 | 0 / 0 | 10:40:47 CST 条件齐备，正式起点成立；新旧模型构成不同，早期 P95 不作回归判定 |
| 2026-08-28 10:40:47–20:50:51 CST（手动触发快照） | /v1/responses / true | 171（143 success + 25 error/500 + 2 error/502 + 1 stream_error） | 2（1 success + 1 error/429） | 83.63% / 50%（legacy n=2） | 15.79% / 0 | ≈50.5s / ≈2.4s（legacy 样本不足，不作回归判定） | 1 | 0 | orchestrator 增量 reserve≈236、commit=143、release=94（抓取边界 ±1）；legacy 增量 reserve=commit=1 | 0 / 0 | 旧 25–70ms 本地转换 502 未复现；StepFun 失败未见本地协议错误，但 Responses 仍需继续观察，不能提前通过 |
| 2026-08-28 10:40:47–20:50:51 CST（手动触发快照） | /v1/messages / true | 0 | 825（815 success + 9 error/502 + 1 error/429） | — / 98.79% | — / 1.09% | — / ≈81.1s | 0 | 0 | legacy 增量 reserve≈833、commit=815、release=19（抓取边界 ±1） | 0 / 0 | 仍无 messages orchestrator 对照样本；StepFun/Kimi 长耗时或上游拒绝与 Responses 本地缺陷分开记录 |
| 2026-08-28 10:40:47–20:50:51 CST（手动触发快照） | /v1/messages / false | 0 | 117（116 success + 1 error/502） | — / 99.15% | — / 0.85% | — / ≈31.3s | — | 0 | legacy 增量 reserve=117、commit=116、release=1 | 0 / 0 | 样本量有限，无新本地错误模式 |
| 2026-08-28 10:40:47–20:50:51 CST（手动触发快照） | /v1/chat/completions / true | 0 | 0 | — / — | — / — | — / — | — | 0 | 无样本 | 0 / 0 | 仍无 canary 流量，不能判定通过 |
| 2026-08-28 10:40:47–2026-08-29 08:21:40 CST（正式窗口累计 21h41m） | /v1/responses / true | 171（143 success + 25 error/500 + 2 error/502 + 1 stream_error） | 2（1 success + 1 error/429） | 83.63% / 50%（legacy n=2） | 15.79% / 0 | ≈55.2s / ≈2.4s（legacy 样本不足） | 1 | 0 | orchestrator 增量 reserve≈236、commit=143、release=94（抓取边界 ±1） | 0 / 0 | 自 20:50 快照无新 Responses 终态；正式起点已成立但本轮零增量不能强化通过结论，旧本地转换 502 仍未复现 |
| 2026-08-28 10:40:47–2026-08-29 08:21:40 CST（正式窗口累计 21h41m） | /v1/messages / true | 0 | 838（828 success + 9 error/502 + 1 error/429） | — / 98.81% | — / 1.07% | — / ≈70.2s | 0 | 0 | legacy 增量 reserve≈846、commit=828、release=19（抓取边界 ±1） | 0 / 0 | 较 20:50 新增 13 success，无新 error；仍无 orchestrator 对照 |
| 2026-08-28 10:40:47–2026-08-29 08:21:40 CST（正式窗口累计 21h41m） | /v1/messages / false | 0 | 125（124 success + 1 error/502） | — / 99.20% | — / 0.80% | — / ≈30.9s | — | 0 | legacy 增量 reserve=125、commit=124、release=1 | 0 / 0 | 较 20:50 新增 8 success，无新 error |
| 2026-08-28 10:40:47–2026-08-29 08:21:40 CST（正式窗口累计 21h41m） | /v1/chat/completions / true | 30（29 success/200 + 1 error/502） | 0 | 96.67% / — | 3.33% / — | ≈76.5s / — | 0 | 0 | orchestrator reserve=31、commit=29、release=2 | 0 / 0 | 首批 Chat Completions canary 样本；唯一 502 为 test/Kimi、约 22.4s，非 StepFun 本地转换错误 |
| 2026-08-28 10:40:47–2026-08-30 08:54:38 CST（正式窗口累计 46h14m） | /v1/responses / true | 285（256 success + 25 error/500 + 3 error/502 + 1 stream_error） | 2（1 success + 1 error/429） | 89.82% / 50%（legacy n=2） | 9.47% / 0 | ≈44.5s / ≈2.4s（legacy 样本不足） | 1 | 0 | orchestrator 增量 reserve≈350、commit=256、release=95（抓取边界 ±1）；legacy 增量 reserve=commit=1 | 0 / 0 | 较 08-29 08:21 新增 113 success、1 error/502；没有 StepFun 本地转换错误证据，仍不能提前通过 |
| 2026-08-28 10:40:47–2026-08-30 08:54:38 CST（正式窗口累计 46h14m） | /v1/messages / true | 0 | 838（828 success + 9 error/502 + 1 error/429） | — / 98.81% | — / 1.07% | — / — | 0 | 0 | legacy 增量 reserve≈846、commit=828、release=19（抓取边界 ±1） | 0 / 0 | 较上次无新增终态，仍无 orchestrator 对照 |
| 2026-08-28 10:40:47–2026-08-30 08:54:38 CST（正式窗口累计 46h14m） | /v1/messages / false | 0 | 125（124 success + 1 error/502） | — / 99.20% | — / 0.80% | — / — | — | 0 | legacy 增量 reserve=125、commit=124、release=1 | 0 / 0 | 较上次无新增终态 |
| 2026-08-28 10:40:47–2026-08-30 08:54:38 CST（正式窗口累计 46h14m） | /v1/chat/completions / true | 66（65 success/200 + 1 error/502） | 2（2 success/200） | 98.48% / 100%（legacy n=2） | 1.52% / 0 | ≈66.0s / ≈9.8s（legacy 样本不足） | 0 | 0 | orchestrator 增量 reserve=36、commit=36、release=0；legacy reserve=commit=2 | 0 / 0 | 较 08-29 08:21 新增 36 success；唯一 orchestrator 502 未增加，仍为 test/Kimi 旧样本 |
| 2026-08-28 10:40:47–2026-08-30 08:54:38 CST（正式窗口累计 46h14m） | /v1/chat/completions / unknown | 0 | 14（14 error/400） | — / — | — / — | — / ≈1.0s | — | 0 | 无 quota 样本 | 0 / 0 | 与 relay/executor 无关的外部调用方请求，按旁注不计入 canary 结论 |
| 2026-08-30 15:53:26–2026-08-31 07:02:16 CST（重启后新窗口） | /v1/responses / true | 236（233 success/200 + 1 error/500 + 2 error/502） | 0 | 98.73% / — | 1.27% / — | ≈26.4s / — | 0 | 0 | orchestrator reserve=235、commit=233、release=2 | 0 / 0 | 上一正式窗口因 15:53:26 CST 重启中断；同 endpoint legacy 尚无样本，**正式观察起点尚未成立** |
| 2026-08-30 15:53:26–2026-08-31 07:02:16 CST（重启后新窗口） | /v1/messages / true | 0 | 61（61 success/200） | — / 100% | — / 0 | — / ≈31.5s | 0 | 0 | legacy reserve=commit=61 | 0 / 0 | 无新 orchestrator 对照样本，不能据 legacy 单路径判定通过 |
| 2026-08-30 15:53:26–2026-08-31 07:02:16 CST（重启后新窗口） | /v1/messages / false | 1（1 success/200） | 28（24 success/200 + 4 error/502） | 100% / 85.71% | 0 / 14.29% | —（orchestrator n=1） / —（legacy P95 未复核） | 0 | 0 | legacy reserve=28、commit=24、release=4；orchestrator reserve=commit=1 | 0 / 0 | 新窗口有 1 个 orchestrator 成功，但无稳定对照；non-stream legacy P95 与 stream 值相同的原始查询未复核，暂不采信，继续观察 |
| 2026-08-30 15:53:26–2026-08-31 07:02:16 CST（重启后新窗口） | /v1/chat/completions / true | 0 | 0 | — / — | — / — | — / — | — | 0 | 无样本 | 0 / 0 | 新窗口尚无 Chat Completions canary 样本 |
| 2026-08-30 15:53:26–2026-08-31 07:02:16 CST（重启后新窗口） | /v1/chat/completions / unknown | 0 | 2（2 error/400） | — / — | — / — | — / — | — | 0 | 无 quota 样本 | 0 / 0 | 外部调用方请求，按旁注不计入 canary 结论 |
| 2026-08-30 15:53:26–2026-08-31 09:26:20 CST（新窗口形成对照后复核） | /v1/responses / true | 236（233 success/200 + 1 error/500 + 3 error/502） | 36（24 success/200 + 3 error/502 + 9 error/503） | 98.73% / 66.67% | 1.27% / 33.33% | ≈26.4s（截至 07:02） / ≈27.3s（2h 窗口） | 0 | 0 | orchestrator reserve=236、commit=233、release=3；legacy reserve=33、commit=24、release=9 | 0 / 0 | 09:15 后新增 legacy 对照；快照当时 orchestrator success 零增量。后续 09:31:45–09:32:00 出现新 orchestrator success，正式起点定为 2026-08-31 09:31:45 CST。09:14 test/Kimi Responses 上游 405 后走 Chat fallback：1 成功、9 失败，失败为上游 500/No available channel，非本地 adaptor 转换错误 |
| 2026-08-30 15:53:26–2026-08-31 09:26:20 CST（新窗口形成对照后复核） | /v1/messages / true | 0 | 61（61 success/200） | — / 100% | — / 0 | — / ≈31.5s | 0 | 0 | legacy reserve=commit=61 | 0 / 0 | 较 07:02 无新增终态 |
| 2026-08-30 15:53:26–2026-08-31 09:26:20 CST（新窗口形成对照后复核） | /v1/messages / false | 1（1 success/200） | 28（24 success/200 + 4 error/502） | 100% / 85.71% | 0 / 14.29% | —（orchestrator n=1） / —（legacy P95 未复核） | 0 | 0 | legacy reserve=28、commit=24、release=4；orchestrator reserve=commit=1 | 0 / 0 | 较 07:02 无新增终态 |
| 2026-08-30 15:53:26–2026-08-31 09:26:20 CST（新窗口形成对照后复核） | /v1/chat/completions / true | 0 | 1（1 success/200） | — / 100% | — / 0 | — / —（legacy n=1，P95 NaN） | — | 0 | legacy reserve=commit=1 | 0 / 0 | 新增 1 个 legacy success；尚无 orchestrator 对照 |
| 2026-08-30 15:53:26–2026-08-31 09:26:20 CST（新窗口形成对照后复核） | /v1/chat/completions / unknown | 0 | 2（2 error/400） | — / — | — / — | — / — | — | 0 | 无 quota 样本 | 0 / 0 | 外部调用方请求，按旁注不计入 canary 结论 |
| 2026-09-01 10:43:15–2026-09-02 10:48 CST（第五次正式窗口最终快照） | /v1/messages / true | ≈98（success） | ≈179（success 176 + error/502 3） | 100% / 98.32% | 0 / 1.68% | **48.3s / 28.46s（+69.7%）** | 0 | 0 | 无（orchestrator reserve≈99、commit≈98、release≈1；legacy reserve≈178、commit≈176、release≈2） | 0 / 0 | **FAIL：P95 超过 20% 回滚线；成功率、账务与协议指标未见异常** |
| 2026-09-02 10:50:06 CST（回滚后） | /v1/responses / true | 0（flag=false） | 2（success） | — / 100% | — / 0 | — / —（早期 n=2） | 0 | 0 | legacy reserve=commit=2 | 0 / 0 | executor 已关闭，首批自然流量全部回到 legacy |

旁注：`/v1/chat/completions` legacy 路径同期出现 6 个 error（stream=unknown，HTTP 400），
经确认为**第三方调用方请求所致**（外部 caller 侧问题），与 relay 路径及 executor 迁移无关，
不计入本观察任何结论。

同时保留部署版本、容器重启时间、flag / allowlist 摘要数量和对应 Prometheus 截图或查询导出。
原始 token 不进入文档或日志。

### 中断记录 1：2026-08-25 11:46:52 CST 重新部署

- 事件：发布新镜像（构建于 2026-08-25 11:45 CST），relay-gateway 容器 11:46:52 CST
  重建（ExitCode=0，优雅退出，非崩溃 / OOM）；进程内计数器清零。
- 重启后确认：`RELAY_ORCHESTRATOR_ENABLED=true`，allowlist 2 个 hash，启动后 28min 内
  orchestrator 已有 3 个 success 样本，reserve=commit，无 5xx / failover。
- **待确认**：`/v1/messages` 流式的 legacy 对照样本在新起点后尚未出现（§1 条件 4），
  待其出现后记为正式新起点；暂以 2026-08-25 11:47 CST 为候选起点，预计新终点
  2026-09-01 11:47 CST。
- **重启前异常（已定性）**：08:30–11:46 CST 旧容器上 `/v1/messages` orchestrator 流式出现
  503×约 120、502×6（result=error），全部集中在重启前 3 小时；08:30 CST 检查时 5xx 为零。
  经确认为**上游供应商报错，非本项目 / executor 路径引入**，不构成 §5 回滚条件；
  作为环境噪声记录，不计入 orchestrator 稳定性结论。新窗口内若 5xx 复现仍需先区分上游与自身。

### 中断记录 2：2026-08-26 11:16:25 CST 重新部署

- 事件：再次发布新镜像（构建于 2026-08-26 11:14:43 CST，image id e82ab380），relay-gateway
  容器 11:16:25 CST 重建（ExitCode=0，优雅退出）；进程内计数器再次清零。2026-08-25 11:47
  起的窗口仅运行约 24h 即中断。
- **版本口径**：镜像构建时间（11:14）早于 `afb9f70`（fix playground issues）提交时间（11:29），
  但构建时工作区已包含该修复全部改动，线上运行内容等同 HEAD（afb9f70）。已确认，勿按
  时间戳误判为旧版本。
- 重启后确认（11:52 CST）：`RELAY_ORCHESTRATOR_ENABLED=true`，allowlist 2 个 hash；
  36min 内 messages orchestrator 21 success，failover 0。
- **新进展**：`/v1/responses` 首次出现 orchestrator 样本（36min 内 40 success），
  §5 覆盖面改善；同 endpoint legacy 仍有 42 success + 1 error（500×1），可作协对照。
- 早期信号（样本小，仅记录）：responses orchestrator P95 56s vs legacy 35.6s（+57%），
  若窗口后期仍超 20% 回归线需按 §5 处理；`/v1/chat/completions` orchestrator 恢复命中（1 个）。
- 候选新起点 2026-08-26 11:17 CST，预计新终点 2026-09-02 11:17 CST；
  legacy 对照（messages 流式）自 8-25 起持续为零，§1 条件 4 待解。

### 2026-08-27 日检结论、归因与已执行回滚

- 固定求值时点：2026-08-27 11:55:18 CST；Prometheus 3.6.0，查询区间严格为此前
  24 小时。`increase` 的小数来自抓取边界外推，表中请求数按最接近整数留证。
- 部署连续性：relay-gateway 仍运行 image `e82ab380`，启动于 2026-08-26 11:16:25 CST，
  `RestartCount=0`；`RELAY_ORCHESTRATOR_ENABLED=true`，allowlist 摘要数量为 2，未记录原始 hash。
- `/v1/responses` orchestrator 的 502 出现在以 15:16、16:16、17:16、18:16 CST 为
  求值点的连续四个 `[1h]` 桶，分别约 2、2、34、14 个；同期 legacy 仍有成功样本。
  18:16 CST 后 orchestrator 样本归零，不能把后续 502 为零解释成恢复。
- quota 生命周期闭合：responses `reserve≈154 = commit≈87 + release≈66 + 差额≈1`；
  messages 流式与非流式也各只剩约 1 个抓取边界 / 在飞差额。`quota_error=0`，failover
  switched / exhausted 均为 0，未见 reservation 卡单。
- billing / usage attribution 只读交叉核对：固定窗口内两边均为 1,797 个已结算请求；
  `stream=false/channel=143`、`stream=true/channel=846`、
  `stream=true/subscription=808` 完全一致。billing endpoint 进一步分解为 messages
  `channel 143(false)+759(true)、subscription 582(true)`，responses
  `channel 87(true)、subscription 226(true)`；stream、channel / subscription account 归因未见不一致。
- 判定：responses orchestrator 成功率比 legacy 低 37.86 个百分点，5xx 高 37.15 个百分点，
  P95 回归约 449%，同时 `/v1/chat/completions` 在日检窗口没有样本，当前窗口不满足 §5
  通过条件。
- 原始请求明细在用户关注的约 147 次口径中进一步分解：94 次成功由 `test` API-key 渠道
  `Kimi-K3` 贡献 87 次、订阅账号 4 的 `glm-5.3` 贡献 7 次；53 次 502 中，42 次选择
  `StepFun` / `step-explore`，11 次选择 `test` / `Kimi-K3`。Prometheus 固定窗口因抓取边界
  外推显示约 140 次、87 次成功，两种口径不可直接混加。
- `StepFun` 42 次失败已确认是本地协议兼容缺口，不是 StepFun 上游故障：orchestrator 把
  `/v1/responses` 标为 `responses` 入站格式，但 Anthropic API-key adaptor 当时只接受原生
  `anthropic_messages`，在发起 HTTP 请求前即返回“不支持 responses”；这些请求多数在
  25–70ms 内结束，随后被映射为 502。
- `test` / `Kimi-K3` 的 11 次 502 多在 156–299s 后结束，形态符合上游或网络超时；当时
  没有留存可安全展示的上游响应体，不能进一步断言具体供应商错误。该问题与 StepFun 的
  本地即时转换失败分开处理。
- Claude 通过线上 `/v1/messages` 调用 `step-explore` 时出现的 `Error editing file` 属于另一条
  Messages / 工具调用链路；当前证据不能证明与 Responses adaptor 缺口同源，需以后凭该请求的
  时间点或 request ID 核对模型输出和工具执行结果。
- 12:45:56 CST 已备份生产 compose `.env`，设置 `RELAY_ORCHESTRATOR_ENABLED=false` 并只重建
  relay-gateway；新容器于 12:46:02 CST 启动，`RestartCount=0`。随后指标确认
  `/v1/messages` 与 `/v1/responses` 均产生 `execution_path="legacy"` 的新终态样本，回滚生效。
- `test` 渠道暂不直接禁用：路由核对显示 `Kimi-K3` 等多个模型只有该渠道的精确生产能力；
  Kimi 订阅账号仅发布 `k3`、`k3-256k` 等别名，没有可无损替换的现成路由。修复恢复时继续
  使用既有 2-token allowlist 控制暴露面，不在本次事故处理中冒险改写模型映射。

### 中断记录 3：2026-08-27 回滚、修复部署与 canary 恢复

- 12:46 CST 按 §5 关闭 orchestrator 后确认两个主入口均重新产生 legacy 终态样本；此前从
  2026-08-26 11:17 CST 开始的观察窗口终止。
- 修复内容：Anthropic API-key adaptor 新增 Responses→Anthropic Messages 请求转换、Anthropic
  非流式响应→Responses 转换和 SSE 双向终态转换；继续移除第三方 Messages 服务不普遍支持的
  `thinking`、`output_config` 和 server-tool `type` 扩展。请求、非流式、流式以及 orchestrator
  forwarder 到 `/v1/messages` 的回归测试均已补齐。
- 部署前验证：`go test ./internal/adaptor ./internal/server` 通过；不依赖外部进程的服务端完整
  测试集通过。`go test ./...` 仅有 `test/e2e/suite` 因本机未启动 3000 / 9001 / 9004 服务失败，
  其余包全部通过。
- 15:39:40 CST 部署 amd64 镜像 `178eae0c3cd9`，旧镜像保留为
  `docker-compose-relay-gateway:rollback-20260827-153929`；先保持 flag=false 完成启动检查。
  15:40:24 CST 以原 2-token allowlist 恢复 flag=true，容器 `RestartCount=0`。
- canary 初始结果：前 2 分钟 `/v1/messages` orchestrator 产生 5 个 success / 200，均包含真实
  `StepFun` / `step-explore` 成功请求；尚无新的 `/v1/responses` orchestrator 终态样本，因此这只能
  证明部署与 Messages 路径正常，不能把 15:40 CST 直接记作 §1 的正式新起点。须等待 Responses
  真实样本并确认 StepFun 不再出现 25–70ms 的本地转换 502。
- 16:20 CST 复核：`/v1/responses` 流式 orchestrator 已完成 50 success / 200、1 error / 502；
  quota `reserve=51 = commit_async=50 + release=1`，无差额、`quota_error` 或 failover。50 个成功中
  至少 4 个由 `StepFun` / `step-explore` / channel 9 产生，首个在 16:06:47 CST 完成，确认
  Responses→Anthropic API-key 修复已真实命中上游并返回 Responses 终态。
- 唯一 502 同样选择 StepFun/channel 9，发生于 16:08:12 CST，耗时 569ms；它不符合旧缺陷
  25–70ms、HTTP 请求发出前失败的特征，日志中也未再出现 `inbound format "responses" is not
  supported` / adaptor conversion 错误。但现有脱敏日志没有保留失败阶段或上游状态，暂记为
  **未归因的单次 502**，不能强行认定为上游。
- canary 累计 P95：Responses 流式约 89.0s，Messages 流式约 57.3s；容器运行正常、
  `RestartCount=0`。本次重启后尚无同 endpoint / stream 的 legacy 新样本，故仍不满足 §1
  条件 4，暂不建立正式 7 天起点；继续观察该 502 是否复现以及 P95 是否随样本增长收敛。
- 16:55 CST 心跳复核：Responses 流式累计 106 success / 200、2 error / 502，P95 已从
  89.0s 收敛到 59.7s；相对回滚前 legacy 历史快照 54.6s 高约 9.3%，低于 §5 的 20% 线，
  但该比较并非同一重启窗口，只作早期参考。quota `reserve=109 = commit_async=106 +
  release=2 + 1 个在飞`，仍无 `quota_error` 或 failover。
- StepFun/channel 9 的 Responses 成功已增至 14 个；16:08:12 CST 的单次 569ms 502 后连续
  约 47 分钟无 StepFun 新失败，也没有 adaptor conversion / unsupported-format 日志，旧本地
  兼容缺口未复现。新增的第二个 502 来自 `test`/channel 1 的 `Kimi-K3`，发生于 16:30:39 CST、
  耗时 7.8s，应与 StepFun 修复分开记录；现有脱敏日志仍不足以确认其具体上游错误。
- 同期 Messages 流式 123 success / 200、非流式 2 success / 200，均无 error；
  `/v1/chat/completions` 仍无 canary 样本。容器继续 `RestartCount=0`。StepFun 修复的 PATCH
  发布前短窗门槛已满足，但正式 7 天观察仍因缺少重启后的 legacy 对照而尚未起算。

### 2026-08-28 09:48 CST 手动触发观察

- 生产容器仍是 2026-08-27 15:40:24 CST 启动的修复实例，`RestartCount=0`、状态 running；
  `RELAY_ORCHESTRATOR_ENABLED=true`，allowlist 仍为 2 个摘要值。期间没有部署或 counter 重置。
- 修复容器累计 `/v1/responses` 流式 orchestrator 364 个终态：358 success / 200、5 error /
  502、1 error / 500，成功率 98.35%、5xx 比例 1.65%、P95 52.9s。固定 `[24h]` 查询另有
  重启前 legacy success≈33、429×1、P95 29.1s；这些样本不在修复重启后的同一窗口，不能作为
  §1 条件 4 的有效对照。修复重启后 legacy counter 仍为零，**正式观察起点尚未成立**。
- 脱敏 usage 日志中 Responses 成功样本按渠道 / 模型分为：StepFun channel 9
  `step-explore` 151、test channel 1 `kimi-k3` 98、订阅账号 4 `glm-5.3` 115。日志计数晚于
  09:48 指标快照，故只用于确认真实路由覆盖，不与该快照的 358 个 success 直接相加。
- 7 个普通路由失败全部发生在 16:08–17:57 CST，之后至本次检查没有新增。按安全字段和
  Prometheus endpoint counter 对齐：Responses 的 6 个失败包括 StepFun 3 个
  （569ms/502、240.8s/500、9.5s/502）和 test/Kimi 3 个（7.8s、5.4s、236.2s，均为
  502）；Messages 的 1 个普通失败是 StepFun 29.8s/502，另有 1 个流式连接
  `stream_error`。短错误与长耗时错误均单独保留，现有脱敏日志不足以断言具体上游响应原因。
- 修复容器日志中 `inbound format "responses" is not supported`、Responses→Anthropic adaptor
  conversion 错误和 `Error editing file` 均为 0；没有再出现旧缺陷的 25–70ms、本地发起 HTTP
  前即失败形态。最短的 StepFun Responses 失败仍是先前已记录的 569ms 单次事件。
- quota 在首个快照出现 1 个在飞差额，随后复核已闭合：Responses
  `reserve=367 = commit_async 362 + release 5`，Messages 流式
  `reserve=153 = commit_async 151 + release 2`；`quota_error=0`，failover switched / exhausted
  均为 0。`/v1/chat/completions` 修复重启后仍无样本。
- 判定：未触发 §5 回滚。StepFun 的旧本地协议兼容回归未复现，但当前成功率仍受 6 个已归因到
  渠道路由阶段的 Responses 5xx 影响，且缺少同重启窗口 legacy 对照与 Chat Completions 样本；
  因此只能继续 canary 观察，不能宣布 7 天窗口通过。

### 中断记录 4：2026-08-28 10:33 CST 缩减 allowlist

- 10:32:59 CST 先备份生产 compose `.env`，随后按用户指令从 allowlist 精确移除 1 个摘要项；
  未输出或记录原始 token / 摘要值，未修改数据库渠道、模型映射或其他服务配置。
- 只执行 `docker compose up -d --no-deps --force-recreate relay-gateway`；新容器于
  10:33:09 CST 启动，状态 running、`RestartCount=0`、ExitCode=0，启动后未见 panic、fatal、
  配置加载或端口绑定错误。
- 运行时确认 `RELAY_ORCHESTRATOR_ENABLED=true`，allowlist 摘要数量由 2 降为 1。此次重建使
  10:33 CST 前的进程内 counter 窗口再次中断，上一候选窗口不能继续累计。
- 首次 Prometheus 抓取已出现 `/v1/messages`、`stream=false`、`execution_path="legacy"`、
  `result="success"`、HTTP 200 共 1 个新终态，确认移除后的流量可以回落旧路径；同一新容器
  尚无 orchestrator 终态样本，**正式观察起点尚未成立**。须等待至少一个 endpoint / stream
  同时具备 orchestrator 与 legacy 新样本后重新记录候选起点。
- 10:37:55 CST 连续两个抓取周期复核：Responses 流式已有 orchestrator success / 200 共
  20 个，Messages legacy 已有流式 success / 200 共 8 个、非流式 success / 200 共 5 个及
  error / 502 共 1 个；但 Responses 尚无 legacy，Messages 尚无 orchestrator。新旧流量已经
  同时存在，却仍未在同一 endpoint / stream 形成对照，故正式起点依然不能建立。
- 10:40:45–10:40:47 CST 使用已移出 allowlist 的内部 token 发起 1 个最小流式
  `/v1/responses` / `step-explore` 受控请求；HTTP 200、Content-Type 为 `text/event-stream`，
  约 2.4s 完成，包含唯一 `response.completed` 与 `[DONE]`，没有 response.failed / error 事件。
  原始 token、响应内容和 usage 明细均未输出或写入本文。
- 10:41 CST Prometheus 抓取确认同一容器、同一 `/v1/responses` / `stream=true` 已同时具有
  orchestrator success / 200 共 39 个和 legacy success / 200 共 1 个；orchestrator P95
  约 20.3s，legacy 只有 1 个约 2.4s 的样本，不能据此比较延迟回归。quota 中 legacy
  `reserve=commit=1`，orchestrator `reserve=40、commit=39`，差额 1 为抓取时在飞请求；
  `quota_error=0`、failover 为 0。
- 脱敏 usage 日志显示，本次重启后的 orchestrator Responses 成功流量为 `k3`，首个完成于
  10:34:47 CST；本次受控 legacy 对照为 `step-explore`，于 10:40:47 CST 完成。按 §1 明确的
  endpoint / stream 口径，最后一个起点条件在 10:40:47 CST 满足，故第四次正式窗口从该时刻
  起算，预计 2026-09-04 10:40:47 CST 结束。由于新旧模型构成不同且当前 legacy 样本仅 1 个，
  这只能建立正式计时起点，不能提前判定 StepFun 或整体灰度通过；后续日检仍须分别记录模型
  归因和样本量限制。

### 2026-08-28 20:50 CST 手动触发复核

- 通过生产 Prometheus 读取 10:40:47 CST 起点快照与 20:50:51 CST 当前快照，并按
  `endpoint / stream / execution_path / result / status` 计算增量。Responses 流式
  orchestrator 增量为 171 个终态（143 success/200、25 error/500、2 error/502、1
  stream_error/200）；同一 endpoint 的 legacy 仅新增 1 success/200 与 1 error/429，
  不能作为稳定成功率或 P95 对照。orchestrator P95 约 50.5s，legacy 单样本约 2.4s；
  现有日志未显示旧的本地即时转换模式，具体上游失败阶段仍不完全可见。
- Messages 流式仍全部落在 legacy（815 success/200、9 error/502、1 error/429）；非流式
  legacy 新增 116 success/200、1 error/502；两种 stream 均无 orchestrator 样本。
  `/v1/chat/completions` 流式与非流式均无新样本。故正式窗口继续计时，但 endpoint 覆盖和
  同窗对照仍不完整，不能提前宣告通过。
- quota 增量保持闭合：Responses orchestrator `reserve≈236、commit=143、release=94`
  （抓取边界 ±1）；Messages legacy 流式 `reserve≈833、commit=815、release=19`，非流式
  `reserve=117、commit=116、release=1`。`quota_error=0`，failover switched / exhausted
  均为 0，未发现 reservation 卡单或重复结算。
- 脱敏 usage 日志共确认 Responses 成功路由覆盖：`k3` 112、test/Kimi 30、StepFun/
  `step-explore` 1；Messages 成功覆盖 `kimi-k3` 493、StepFun/`step-explore` 7（流式）及
  116（非流式）、`deepseek-v4-pro-0813` 4。路由错误日志共 40 条，其中 StepFun 2 条
  （约 1.85s 与 26.8s）、test/Kimi 36 条（以 10–60s 为主）、订阅 `glm-5.3` 2 条均为
  上游 429；这些记录未输出请求内容、token、hash 或上游响应体。
- relay-gateway 自正式起点以来日志中 `inbound format "responses" is not supported`、
  Responses→Anthropic adaptor conversion、`Error editing file`、panic/fatal 均为 0；未再
  出现 25–70ms、本地发起 HTTP 前失败的旧缺陷。当前 flag 仍为 `true`、allowlist 摘要数量
  为 1，容器 `RestartCount=0`。本次复核未达到 §5 回滚线，继续观察至正式窗口结束或出现新的
  稳定性/计费/协议兼容回归。

### 2026-08-29 08:21 CST 日检

- relay-gateway 仍是 2026-08-28 10:33:09 CST 启动的同一容器，状态 running、
  `RestartCount=0`；flag 仍为 `true`，allowlist 摘要数量为 1。没有部署、重启或 counter
  重置，第四次正式窗口连续运行约 21 小时 41 分钟。
- `/v1/responses` 流式的起点至当前累计值与 20:50 快照相同：orchestrator 143 success/200、
  25 error/500、2 error/502、1 stream_error/200，legacy 1 success/200、1 error/429。
  本轮没有新的 Responses orchestrator 终态，不能据零增量推断恢复或通过；但正式观察起点
  已于 2026-08-28 10:40:47 CST 成立，不需要重新起算。约 21 小时窗口的 orchestrator P95
  为 55.2s，legacy 仍只有单个约 2.4s 的成功样本，延迟对照仍不具统计意义。
- `/v1/chat/completions` 流式首次出现 orchestrator 终态：29 success/200、1 error/502，
  P95 约 76.5s；legacy 仍为 0。脱敏路由日志将唯一错误归到 test/Kimi channel 1，耗时约
  22.4s；同窗 29 个成功 usage 也均来自该模型/渠道。该错误与 StepFun Responses 本地转换
  缺陷分开记录，现有安全字段不足以进一步断言具体上游响应原因。
- Messages 流式较 20:50 快照新增 13 个 legacy success，非流式新增 8 个 legacy success，
  两者均无新 error；仍没有 Messages orchestrator 样本。昨晚快照后的脱敏 usage 日志另确认
  StepFun/`step-explore` 非流式成功 8 个、`deepseek-v4-pro-0813` 流式成功 1 个。
- quota 保持闭合：Chat Completions orchestrator `reserve=31、commit=29、release=2`；
  Responses 与 Messages 的累计 commit/release 关系没有恶化。`quota_error=0`，failover
  switched / exhausted 均为 0。昨晚快照后的日志中 unsupported Responses 入站格式、adaptor
  conversion、`Error editing file`、panic/fatal 仍均为 0；没有 25–70ms 的 StepFun 本地转换
  502 新样本。
- 判定：新增 Chat Completions 覆盖是正向进展，但缺少同 endpoint legacy 对照，Responses
  本轮又没有新终态，不能判定 7 天窗口通过。唯一新增 502 是 test/Kimi 的 22.4s 路由错误，
  未形成稳定的本地协议兼容回归；本次未达到 §5 回滚线，继续观察。

### 2026-08-30 08:54 CST 日检

- relay-gateway 仍是 2026-08-28 10:33:09 CST 启动的同一容器，状态 running、`RestartCount=0`；
  flag 仍为 `true`，allowlist 摘要数量为 1。正式窗口已连续运行约 46 小时 14 分钟，没有部署、
  重启或 counter 重置。
- `/v1/responses` 流式较上一日新增 113 个 orchestrator success/200 和 1 个 error/502，
  累计为 256 success、25 error/500、3 error/502、1 stream_error；legacy 仍仅 1 success
  与 1 error/429。P95 约 44.5s；同窗 legacy 只有 2 个终态，不能作稳定延迟对照。脱敏日志
  未发现 StepFun 路由错误或 25–70ms 本地转换失败。
- `/v1/chat/completions` 流式累计 65 success/200、1 error/502（P95 约 66.0s），并新增
  legacy 流式 2 个 success/200；非流式 legacy 1 个 success/200。另有 14 个 `stream=unknown`
  的 legacy 400，符合外部调用方请求问题，单列不计入 canary。新 usage 日志中的 36 个 Chat
  成功均为 test/Kimi channel 1；未见新的 StepFun Responses 成功或失败。
- Messages 两种 stream 较上次无新增终态：流式累计 828 success、9 error/502、1 error/429，
  非流式 124 success、1 error/502，仍无 orchestrator 样本。quota 当前关系未恶化：Responses
  `reserve=389、commit=294、release=95`，Chat Completions orchestrator `reserve=67、
  commit=65、release=2`；`quota_error=0`、failover switched / exhausted 均为 0。
- 昨日日检后的脱敏 routing 日志仅有 1 条 Kimi channel 1 错误（约 744ms），没有 StepFun 错误；
  relay 日志中 `inbound format "responses" is not supported`、Responses→Anthropic adaptor
  conversion、`Error editing file`、panic/fatal 均为 0。由于 Responses 本轮仍有 502 且 Chat
  legacy 对照样本很小，不能提前宣布窗口通过；但未见稳定本地协议回归，本次不触发 §5 回滚，
  继续观察至 2026-09-04 10:40:47 CST。

### 2026-08-30 16:36 CST 观察快照

- relay-gateway 于 2026-08-30 15:53:26 CST 完成重启，容器状态 running、`RestartCount=0`；
  flag 保持 `RELAY_ORCHESTRATOR_ENABLED=true`，allowlist 摘要数量为 1。重启后 counter 重新累计，
  运行约 43 分钟。
- 各 endpoint 指标采样（Prometheus 累计）：
  - `/v1/responses`（流式）：orchestrator 10 success/200、1 error/500；P95 位于 5–10s 区间。
  - `/v1/messages`（非流式）：orchestrator 1 success/200；legacy 24 success/200、4 error/502。
  - `/v1/messages`（流式）：legacy 32 success/200。
- Quota 闭环与 Failover：
  - `/v1/responses` orchestrator 流式：`reserve=13、commit=12`（含在途流）。
  - `/v1/messages` orchestrator 非流式：`reserve=1、commit=1`。
  - `/v1/messages` legacy 非流式：`reserve=28、commit=24、release=4`（release 数量与 4 次 502 错误完全吻合，配额精确释放）。
  - `/v1/messages` legacy 流式：`reserve=32、commit=32`。
  - `quota_error=0`，failover 计数未见异常，无卡单与泄漏。
- 稳定性复核：relay 日志中无 panic/fatal，无旧协议转换错误（如 unsupported Responses 入站等），异步计费结算正常排队提交。未达 §5 回滚线，继续保持观察。

### 中断记录 5：2026-08-30 15:53 CST 重启

- 2026-08-30 15:53:26 CST 发现 relay-gateway 已由新容器接替；当前容器状态 running、
  `RestartCount=0`，本次复核未执行重启或配置变更。由于进程内 Prometheus counter 已重置，
  2026-08-28 10:40:47 CST 起的正式窗口在此中断，不能把此前累计时长延续到新容器。
- 重启后截至 2026-08-31 07:02:16 CST，Responses 流式 orchestrator 有 233 success/200、
  1 error/500、2 error/502，quota `reserve=235、commit=233、release=2`；同 endpoint legacy
  为零。Messages 非流式有 1 个 orchestrator success/200 与 24 success/200、4 error/502
  legacy，流式仅 61 success/200 legacy；Chat Completions 只有 2 个 unknown-stream/400。
- 脱敏 usage 日志确认重启后 Responses 成功均为 test/Kimi channel 1；StepFun/`step-explore`
  有 24 个 Messages 非流式成功。路由错误为 StepFun 4 条（10–60s）和 test/Kimi 2 条（<10s），
  未出现 25–70ms 的本地即时失败形态。日志中的 unsupported Responses 入站格式、adaptor
  conversion、`Error editing file`、panic/fatal 均为 0，failover switched / exhausted 亦为 0。
- 结论：新容器尚未在 Responses 同 endpoint/stream 形成 legacy 对照，按 §1 **正式观察起点尚未成立**；
  需重新等待 Responses orchestrator 与 legacy 同时出现后再开始 7×24 小时计时。当前没有稳定的
  本地协议兼容回归或计费异常，不触发 §5 回滚；不要把这次重启当作发布或修复动作。

### 2026-08-31 09:26 CST 观察快照

- 容器与配置：relay-gateway 仍为 2026-08-30 15:53:26 CST 启动的容器，状态 running，
  `RestartCount=0`、无 OOM；`RELAY_ORCHESTRATOR_ENABLED=true`，allowlist 摘要数量为 1。
  Prometheus 抓取 `up=1`，本快照未发现 counter 重置。
- `/v1/responses` 流式累计：orchestrator 233 success/200、1 error/500、3 error/502，
  较 07:02 仅新增 1 个 502；legacy 24 success/200、3 error/502、9 error/503。legacy 样本在
  09:15–09:25 之间出现，同 endpoint/stream 对照已经形成。2 小时窗口 P95 约为
  orchestrator 2.4s、legacy 27.3s，但两侧流量时间与模型构成不同，不能作为回归结论。
- Quota 闭环：orchestrator `reserve=236、commit=233、release=3`；legacy
  `reserve=33、commit=24、release=9`，legacy release 与 3×502 + 9×503 数量吻合；
  `quota_error=0`，failover switched / exhausted 均为 0。
- 脱敏日志：09:14:19–09:14:48 CST `test` / Kimi Responses 上游先返回 405，随后 Responses→
  Chat fallback 1 次 200 成功、9 次 500 失败；失败响应均为上游 `No available channel for model
  kimi-k3 under group fireworks`。全容器仅这 9 条 WARN，无 ERROR/FATAL/panic；unsupported
  Responses 入站格式、adaptor conversion、`Error editing file` 均为 0。
- 复核更新：09:31:45 时 orchestrator success 仍为 233，legacy 对照已存在；09:32:00 抓取时
  orchestrator success 增至 235。按 §1 从条件齐备后的第一条新 orchestrator 样本开始，取首个
  样本所在的 15 秒抓取区间起点 **2026-08-31 09:31:45 CST** 作为第五次正式窗口起点，预计终点
  **2026-09-07 09:31:45 CST**。
- 09:35 CST 复核：orchestrator success 已增至 242，quota `reserve=246、commit=242、release=3`；
  legacy 为 39 success、3 error/502、9 error/503，quota `reserve=48、commit=39、release=9`。
  当前失败归因仍指向 test/Kimi 上游路由/兼容层，而非本地 executor 转换缺陷；未达到 §5 回滚线。

### 中断记录 6：2026-08-31 22:01:22 CST 重启

- 2026-09-01 10:10 CST 日检发现 relay-gateway 当前容器启动时间为 2026-08-31 22:01:22 CST，
  状态 running、`RestartCount=0`、ExitCode=0，镜像 digest 为 `25d0a278d121` 开头；本次观察
  未执行重启或配置变更，重启原因在只读检查中未确认。
- 重启使进程内 Prometheus counter 清零，因此 2026-08-31 09:31:45 CST 起的第五次正式窗口中断，
  不能把重启前累计时长延续到当前窗口；本事件不作为发布或修复动作记录。
- 重启后确认 `RELAY_ORCHESTRATOR_ENABLED=true`、allowlist 摘要数量为 1，Prometheus
  `up{instance="relay-gateway:8080"}=1`，`/healthz` 返回 `{"status":"ok"}`。
- 当前进程尚未在 `/v1/responses / true` 或 `/v1/chat/completions / true` 产生终态；
  `/v1/messages / true` 与 `/v1/messages / false` 虽已同时有新旧路径流量，但未形成 Responses
  或 Chat Completions 的同 endpoint / stream 对照，正式新起点尚未成立。

### 2026-09-01 10:18 CST 日检（重启后候选窗口）

- 当前进程自 2026-08-31 22:01:22 CST 起累计 194 个终态：`/v1/messages / true` 的
  orchestrator 为 1 success、1 error/502，legacy 为 139 success、2 error/502；
  `/v1/messages / false` 的 orchestrator 为 2 success，legacy 为 49 success；
  `/v1/responses / true` 与 `/v1/chat/completions / true` 均为 0。流式 P95（当前进程的
  `[12h]` 近似查询）为 messages orchestrator 9.75s、legacy 29.98s；非流式为
  orchestrator 29s、legacy 22s，orchestrator 样本均过小，不作回归判定。
- quota 闭环：messages 流式 orchestrator `reserve=2、commit=1、release=1`，legacy
  `reserve=141、commit=139、release=2`；messages 非流式 orchestrator `reserve=commit=2`，
  legacy `reserve=commit=49`。合计 `reserve=194、commit=191、release=3`，与终态错误数吻合；
  `quota_error=0`，failover switched / exhausted 均为 0。
- billing / usage 只读交叉核对：billing reservations 为 `committed=191、released=3`，
  `billing_ledgers` 的 `consume=191`；ledger 与 usage log 的来源/流式分组完全一致：
  `stream=false/channel=2、subscription=49`，`stream=true/channel=116、subscription=24`。
  当前进程 usage 语义异常、ambiguous settlement 均为 0，异步 billing queue 为 0。
- relay 日志自重启以来 `inbound format "responses" is not supported`、Responses→Anthropic
  adaptor conversion、`Error editing file`、panic/fatal、error-level marker 均为 0；
  未复现旧的本地协议转换错误。`/v1/messages / true` 的单个 orchestrator 502 需继续归因，
  但当前 n=2，尚未构成稳定回滚信号。
- 判定：正式 7 天窗口尚未重新起算；当前不宣布通过、不执行回滚，继续等待同一 endpoint / stream
  形成新旧对照并观察 Responses / Chat Completions 实际命中。若出现稳定协议错误、quota/billing
  不一致或 P95 超过 §5 门槛，按手册关闭 flag 或清空 allowlist。

### 2026-09-01 10:43:15 CST 受控对照与第五次窗口重建

- 使用用户提供的两枚 token 各发送 1 次完全相同的 `POST /v1/responses`、`stream=true`、
  `model=step-explore` 最小请求；原始 token、响应正文和 usage 明细均未输出或写入本文。
  两次请求均 HTTP 200，Content-Type 为 `text/event-stream`，均收到 `response.completed` 与
  `[DONE]`，耗时约 1.80s / 1.39s。
- 15 秒 Prometheus 抓取复核确认同一 `/v1/responses / true` 同时出现 1 个 orchestrator
  success/200 和 1 个 legacy success/200；orchestrator 首个样本出现在 10:43:00 CST，
  legacy 对照于 10:43:15 CST 可见。按 §1“条件齐备后首条 orchestrator 样本”并以抓取区间留证，
  第五次正式窗口重建起点取 **2026-09-01 10:43:15 CST**，预计终点 **2026-09-08 10:43:15 CST**。
- 两条探测的 quota 均为 `reserve=commit=1`，无 release、`quota_error` 或 failover；billing
  ledger 新增 2 条 `consume`，usage log 新增 2 条流式 channel 归因。legacy ledger 的
  `source_kind` 兼容字段为空但 `channel_id` 已填充，orchestrator 行标记为 channel；当前未见
  channel / subscription account 混淆，后续日检继续关注该 legacy 元数据差异。
- 当前样本仅各 1 个，P95 不作回归判定；受控请求建立了计时条件，但不代表 7 天窗口已通过。
  后续按固定 `[24h]` PromQL 日检，并单独保留重启后当前进程窗口的 counter，避免再次跨重启混算。

### 2026-09-02 07:25 CST 日检（正式窗口约 20h42m）

- relay-gateway 仍为 2026-08-31 22:01:22 CST 启动的同一容器，状态 running、`RestartCount=0`、
  ExitCode=0；`RELAY_ORCHESTRATOR_ENABLED=true`、allowlist 摘要数量为 1，Prometheus ready、
  `/healthz` 正常。正式窗口自 2026-09-01 10:43:15 CST 起连续运行，期间未部署、重启或清零 counter。
- 固定 `[24h]` 查询显示：messages legacy 流式 289 success、4 error/502，非流式 88 success；
  messages orchestrator 非流式 1 success，流式 `increase` 为 0；Responses 的两个单样本序列因
  Prometheus 增量边界均返回 0，不能据此视为无样本。当前进程原始 counter 仍明确保留
  `/v1/responses / true` 的 1 orchestrator success 与 1 legacy success（均为起点受控探测），
  正式起点后没有新增 orchestrator 终态；Chat Completions 仍无样本。
- 正式起点后的 billing 只读切片为 `consume=216`、`committed=216`、`released=2`，
  `ledger_dedupe=216` 且无空 dedupe key；usage log 的来源/流式分组与之吻合：
  `stream=false/channel=40`，`stream=true/channel=163、subscription=13`，无未知来源记录。
  当前进程累计 reservations 为 `committed=409、released=5`，与重启后 ledger `consume=409`
  一致；差异来自正式起点前的候选流量，不跨窗口合并。
- `quota_error=0`，failover switched / exhausted 均为 0，异步 billing queue 为 0；usage
  invariant mismatch、ambiguous settlement 均为 0。relay 日志自重启以来 unsupported Responses
  入站格式、adaptor conversion、`Error editing file`、panic/fatal 和 error-level marker 均为 0。
- 判定：正式窗口连续且未触发回滚线，但 endpoint 覆盖仍不完整（Messages orchestrator、Chat
  Completions 无正式窗口样本），Responses 只有起点单样本，不能提前通过；继续观察至
  2026-09-08 10:43:15 CST。

### 2026-09-02 10:45–10:48 CST 最终日检（正式窗口约 24h05m）

- relay-gateway 仍为 2026-08-31 22:01:22 CST 启动的同一容器，镜像 digest 仍为
  `25d0a278d121` 开头，`RestartCount=0`、ExitCode=0、无 OOM；flag、allowlist、模型映射、
  timeout 和 Prometheus 抓取均未在正式窗口内变化。
- 固定 `[24h]` 查询（查询终点 10:45:21 CST，与正式起点相差约 2 分钟）显示
  `/v1/messages / true` 新出现约 98 个 orchestrator success，同窗 legacy 为约 176 个
  success 和 3 个 error/502。新旧成功率约 100% / 98.32%，5xx 约 0 / 1.68%；但 P95 为
  **48.3s / 28.46s**，orchestrator 相对回归约 **69.7%**，明确超过 §5 的 20% 回滚线。
- 同一查询中 `/v1/responses / true` 有约 109 个 orchestrator success、P95 约 66.6s；
  trailing `[24h]` 的 legacy `increase` 为 0，当前进程原始 counter 仍保留起点受控 legacy
  success 1 个，故该 endpoint 不作延迟对比。`/v1/chat/completions` 仍无正式窗口样本。
- quota 完整闭合：messages orchestrator `reserve≈99 = commit≈98 + release≈1`，messages
  legacy `reserve≈178 = commit≈176 + release≈2`，Responses orchestrator
  `reserve=commit≈109`；`quota_error=0`，failover switched / exhausted 均为 0，异步 billing
  queue 为 0。
- 以精确正式起点查询数据库：reservations 为 `committed=425、released=3`，consume ledger
  为 425 条、425 个 distinct dedupe key、空 dedupe key 为 0；billing 与 usage log 的来源/流式
  归因完全一致（非流式 subscription 40，流式 channel 372、subscription 13）。425 条均为
  v1 verified canonical，`cost_mismatch=0`、缺失 source/upstream model 为 0。
- relay 日志自 2026-08-31 22:01:22 CST 起，unsupported Responses、adaptor conversion、
  `Error editing file`、panic/fatal 和 error-level marker 均为 0；没有发现能把延迟超线归因到
  协议转换、计费队列或进程异常的证据。现有低基数指标不含 model/source，不能在不猜测的
  前提下区分 executor 开销与两侧请求构成差异；固定 PromQL 与上述样本即为本次最小复现。
- **最终判定：FAIL**。不因成功率和账务正常而放宽 P95 准入线；先回到 legacy。若后续重启
  executor 观察，必须先补充可比较的延迟 cohort 或证明当前差异来源，并重新建立 §1 起点。

### 2026-09-02 10:50:06 CST 回滚与验证

- 回滚前备份生产 compose `.env` 为 `.env.bak.executor-fail-20260902-1055`，只把
  `RELAY_ORCHESTRATOR_ENABLED` 从 `true` 改为 `false`；allowlist 摘要数量仍为 1，未修改
  canonical producer、模型映射、timeout、数据库或其他服务。
- 仅执行 `docker compose up -d --no-deps --force-recreate relay-gateway`。新容器继续使用
  `sha256:25d0a278d121...` 镜像，10:50:06 CST 启动，`RestartCount=0`、ExitCode=0、无 OOM；
  `/healthz` 正常，Prometheus `up{instance="relay-gateway:8080"}=1`，启动错误标记为 0。
- 回滚后首批 2 个自然 `/v1/responses / true` 请求均记录为 legacy success，quota
  `reserve=commit=2`，failover 为空、异步 billing queue 为 0，确认所有请求已恢复旧路径。
- 后续决策为 **保持 legacy**；不默认开启 executor、不删除旧 handler，也不把本次 FAIL
  拼接进新的 7 天窗口。

## 5. 通过与回滚

7 天窗口通过需同时满足：

- 每个实际承载 allowlisted 生产流量的 endpoint / stream 均有 orchestrator 样本，不能用零样本判定通过；
- orchestrator 成功率不低于同 endpoint / stream 的 legacy 对照，且没有新增稳定错误类型；
- orchestrator P95 相对 legacy 不回归超过 20%；
- `quota_error=0`，没有重复 Commit、成功流 Release、截断流 Commit 或 reservation 卡单；
- failover exhausted 比例没有显著高于 legacy，reason 分布能够解释；
- billing 的 endpoint / stream 归因正确，usage 日志的 stream、channel / subscription account
  归因一致。

出现稳定性错误、计费不一致、P95 超线或协议兼容回归时，清空 allowlist 或关闭
`RELAY_ORCHESTRATOR_ENABLED`，所有请求立即回到 legacy。回滚不删除旧 handler，也不需要数据库
回滚；恢复观察时重新确认 §1 的起点。
