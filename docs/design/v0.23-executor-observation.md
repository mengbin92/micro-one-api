# v0.23 executor 新旧路径 7 天观察手册

> 制定日期：2026-08-24  
> 状态：已回滚、修复并恢复 canary，第三次观察窗口待确认。2026-08-27 日检触发 §5 成功率、
> 5xx 和 P95 回滚线；12:46 CST 关闭生产 orchestrator，15:40 CST 部署修复后以原 2-token
> allowlist 重新开启。原观察窗口作废，须按 §1 重新确认起点。

## 1. 观察范围与计时起点

同一组指标覆盖以下入口：

- `POST /v1/chat/completions`，流式与非流式；
- `POST /v1/responses`，流式与非流式；
- `POST /v1/messages`，流式与非流式；
- `/v1/responses` WebSocket 保留在 `legacy`，但记录为 `stream=true`，不计作 executor
  迁移命中。

观察窗口从下列条件全部满足后的第一条 orchestrator 指标样本开始，记录精确 CST 时间：

1. 新镜像已部署，`RELAY_ORCHESTRATOR_ENABLED=true`，SHA-256 allowlist 非空；
2. 至少一个真实内部 caller 使用 allowlisted token 调用当前主流 SSE 入口；
3. `micro_one_api_relay_executor_requests_total{execution_path="orchestrator"}` 增加；
4. 同 endpoint / stream 仍有 `legacy` 样本可作为对照。

只打开开关、只有 legacy 流量、Prometheus 未抓取或 orchestrator 样本为零，都不能开始计时。
窗口要求连续 7×24 小时；发布新 executor 代码、指标抓取中断超过一个抓取周期，或关闭 flag
后再开启，都要记录中断并重新确认起点。

## 2. 指标口径

| 指标 | 关键标签 | 用途 |
|------|----------|------|
| `micro_one_api_relay_executor_requests_total` | `endpoint,stream,execution_path,status,result` | 请求量、成功率、5xx、流终止和结算错误 |
| `micro_one_api_relay_executor_request_duration_seconds` | `endpoint,stream,execution_path` | 端到端 P95；SSE 包含完整连接生命期 |
| `micro_one_api_relay_executor_quota_outcome_total` | `endpoint,stream,execution_path,outcome` | Reserve / Commit / Release 生命周期 |
| `micro_one_api_relay_executor_failover_total` | `endpoint,stream,execution_path,result,reason` | 切换成功、耗尽和首个失败原因 |

`result` 的稳定值包括 `success`、`error`、`stream_error`、`quota_error`。流式 HTTP 已经写出
200 后仍可能发生协议截断或结算失败，因此不能只用 HTTP status 判断成功；7 天结论必须同时
检查 `result` 和 quota outcome。

## 3. 固定 PromQL

以下查询分别执行 `[24h]` 日检和 `[7d]` 总结；面板可把区间做成变量。

请求量与 terminal result：

```promql
sum by (endpoint, stream, execution_path, result) (
  increase(micro_one_api_relay_executor_requests_total[24h])
)
```

成功率：

```promql
sum by (endpoint, stream, execution_path) (
  increase(micro_one_api_relay_executor_requests_total{result="success"}[24h])
)
/
sum by (endpoint, stream, execution_path) (
  increase(micro_one_api_relay_executor_requests_total[24h])
)
```

5xx 比例：

```promql
sum by (endpoint, stream, execution_path) (
  increase(micro_one_api_relay_executor_requests_total{status=~"5.."}[24h])
)
/
sum by (endpoint, stream, execution_path) (
  increase(micro_one_api_relay_executor_requests_total[24h])
)
```

P95（SSE 表示完整流持续时间，不与非流式混合）：

```promql
histogram_quantile(
  0.95,
  sum by (le, endpoint, stream, execution_path) (
    rate(micro_one_api_relay_executor_request_duration_seconds_bucket[24h])
  )
)
```

quota outcome：

```promql
sum by (endpoint, stream, execution_path, outcome) (
  increase(micro_one_api_relay_executor_quota_outcome_total[24h])
)
```

failover 结果与原因：

```promql
sum by (endpoint, stream, execution_path, result, reason) (
  increase(micro_one_api_relay_executor_failover_total[24h])
)
```

## 4. 每日留证

每天固定时间记录一行；三个 endpoint 应按 `stream` 分组，不把流式和非流式聚合比较。

| 日期 / 窗口 | endpoint / stream | orchestrator 请求数 | legacy 请求数 | 新 / 旧成功率 | 新 / 旧 5xx | 新 / 旧 P95 | stream_error | quota_error | Commit / Release 异常 | failover switched / exhausted | 结论 |
|-------------|-------------------|----------------------|---------------|---------------|-------------|-------------|--------------|-------------|-------------------------|-------------------------------|------|
| 2026-08-24，17:30–22:30 CST（首个 5h） | /v1/messages / true | 16 | 1 | 100% / 100% | 0 / 0 | ≤60s / —（n=1） | 0 | 0 | 无（reserve=commit=16 / 1） | 0 / 0 | 起点确认，首日无异常 |
| 2026-08-25，08:30 CST（累计 15h） | /v1/messages / true | 38 | 1 | 100% / 100% | 0 / 0 | 64.5s / —（n=1） | 0 | 0 | 无（reserve=commit=38，release=0） | 0 / 0 | 无异常 |
| 2026-08-25，08:30 CST（累计 15h） | /v1/chat/completions / true | 43 | 0 | 100% / —（无 legacy 对照） | 0 / — | 26.4s / — | 0 | 0 | 无（reserve≈commit=43） | 0 / 0 | 新增 endpoint 命中，无异常 |
| 2026-08-25，08:30–11:46 CST | /v1/messages / true | — | — | — / — | **503×120 + 502×6（result=error）** / — | — | — | — | — | — | **窗口中断，见下方中断记录** |
| 2026-08-25，11:47–12:15 CST（新起点后 28min） | /v1/messages / true | 3 | 0 | 100% / — | 0 / — | —（样本不足） | 0 | 0 | 无（reserve=commit=3） | 0 / 0 | 新起点初步确认，legacy 对照未出现 |
| 2026-08-25，11:47–14:45 CST（新起点后 3h） | /v1/messages / true | 12 | 0 | 100% / — | 0 / — | 54s / — | 0 | 0 | 无（reserve=commit=12，release=0） | 0 / 0 | 503 未在新镜像复现；legacy 对照仍未出现 |
| 2026-08-25，11:47–16:02 CST（新起点后 4.3h） | /v1/messages / true | 720（success 716 + error 1 + stream_error 3） | 0 | 99.4% / — | 502×1 / — | 47.9s / — | 3 | 0 | reserve=725，commit=716，release=5，差额 4 待观察 | 0 / 0 | 流量显著上升；出现首批 stream_error×3 与 502×1（疑似上游），reserve−commit−release=4 需跟踪是否卡单 |
| 2026-08-25 11:47–2026-08-26 08:59 CST（新起点后 21h） | /v1/messages / true | 962（success 957 + error 1 + stream_error 4） | 0 | 99.5% / — | 502×1 / — | 41.3s / — | 4（+1） | 0 | reserve=965，commit=957，release=7，差额收敛到 1（在飞连接，非卡单） | 0 / 0 | 卡单疑虑解除；stream_error 缓慢累积需继续跟踪 |
| 2026-08-25 11:47–2026-08-26 08:59 CST（新起点后 21h） | /v1/responses / true | 0（orchestrator 无样本） | 269（success 266 + error 3） | — / 98.9% | — / 502×2 | — / 37.7s | — | — | legacy 侧 reserve=commit=266 | — | responses 流量全部在 legacy（疑为 WebSocket，按 §1 不计迁移命中）；若其有 allowlisted 流量，7 天结论需 orchestrator 样本 |
| 2026-08-26，11:17–11:53 CST（二次部署后 36min） | /v1/messages / true | 21 | 0 | 100% / — | 0 / — | 41.1s / — | 0 | 0 | 无（reserve=22，commit=21，差额 1 在飞） | 0 / 0 | 二次部署后正常 |
| 2026-08-26，11:17–11:53 CST（二次部署后 36min） | /v1/responses / true | 40 | 43（success 42 + error 1） | 100% / 97.7% | 0 / 500×1 | 56s / 35.6s（+57%，样本小） | 0 | 0 | 无（reserve=41，commit=40） | 0 / 0 | **responses 首次走 orchestrator**；P95 早期偏高，继续观察是否超 20% 回归线 |
| 2026-08-26，11:17–11:53 CST（二次部署后 36min） | /v1/chat/completions / true | 1 | 0 | 100% / — | 0 / — | 28.5s / — | 0 | 0 | 无（reserve=commit=1） | 0 / 0 | 恢复命中，样本极少 |
| 2026-08-26 11:55–2026-08-27 11:55 CST（固定 `[24h]`） | /v1/messages / true | 1,357（success 1,341 + error 4 + stream_error 12） | 0 | 98.82% / — | 502×4（0.295%） / — | 59.5s / — | 12 | 0 | 无（reserve≈1,360，commit≈1,341，release≈18，差额≈1） | 0 / 0 | 无 legacy 对照；502 与 stream_error 需继续归因 |
| 2026-08-26 11:55–2026-08-27 11:55 CST（固定 `[24h]`） | /v1/messages / false | 145（success 142 + error 3） | 0 | 97.93% / — | 502×3（2.07%） / — | 30.0s / — | — | 0 | 无（reserve≈146，commit≈142，release≈3，差额≈1） | 0 / 0 | 首次留存非流式日检样本；无 legacy 对照 |
| 2026-08-26 11:55–2026-08-27 11:55 CST（固定 `[24h]`） | /v1/responses / true | 140（success 87 + error 53，其中 502×52、400×1） | 226（success 226） | **62.14% / 100%** | **37.15% / 0** | **300s / 54.6s（+449%）** | 0 | 0 | 无（reserve≈154，commit≈87，release≈66，差额≈1） | 0 / 0 | **触发 §5 回滚线：成功率、5xx、P95 三项不合格** |
| 2026-08-26 11:55–2026-08-27 11:55 CST（固定 `[24h]`） | /v1/chat/completions / true | 0 | 0 | — / — | — / — | — / — | — | 0 | 无样本 | 0 / 0 | 24h 窗口零样本；仅部署后首 36min 有 1 个 success，不能判定通过 |

旁注：`/v1/chat/completions` legacy 路径同期出现 6 个 error（stream=unknown，HTTP 400），
经确认为**第三方调用方请求所致**（外部 caller 侧问题），与 relay 路径及 executor 迁移无关，
不计入本观察任何结论。

同时保留部署版本、容器重启时间、flag / allowlist 摘要数量和对应 Prometheus 截图或查询导出。
原始 token 不进入文档或日志。

### 中断记录 1：2026-08-25 11:46:52 CST 重新部署

- 事件：发布新镜像（构建于 2026-08-25 11:45 CST），relay-gateway 容器 11:46:52 CST
  重建（ExitCode=0，优雅退出，非崩溃 / OOM）；进程内计数器清零。
- 重启后确认：`RELAY_ORCHESTRATOR_ENABLED=true`，allowlist 2 个 hash，启动后 28min 内
  orchestrator 已有 3 个 success 样本，reserve=commit，无 5xx / failover。
- **待确认**：`/v1/messages` 流式的 legacy 对照样本在新起点后尚未出现（§1 条件 4），
  待其出现后记为正式新起点；暂以 2026-08-25 11:47 CST 为候选起点，预计新终点
  2026-09-01 11:47 CST。
- **重启前异常（已定性）**：08:30–11:46 CST 旧容器上 `/v1/messages` orchestrator 流式出现
  503×约 120、502×6（result=error），全部集中在重启前 3 小时；08:30 CST 检查时 5xx 为零。
  经确认为**上游供应商报错，非本项目 / executor 路径引入**，不构成 §5 回滚条件；
  作为环境噪声记录，不计入 orchestrator 稳定性结论。新窗口内若 5xx 复现仍需先区分上游与自身。

### 中断记录 2：2026-08-26 11:16:25 CST 重新部署

- 事件：再次发布新镜像（构建于 2026-08-26 11:14:43 CST，image id e82ab380），relay-gateway
  容器 11:16:25 CST 重建（ExitCode=0，优雅退出）；进程内计数器再次清零。2026-08-25 11:47
  起的窗口仅运行约 24h 即中断。
- **版本口径**：镜像构建时间（11:14）早于 `afb9f70`（fix playground issues）提交时间（11:29），
  但构建时工作区已包含该修复全部改动，线上运行内容等同 HEAD（afb9f70）。已确认，勿按
  时间戳误判为旧版本。
- 重启后确认（11:52 CST）：`RELAY_ORCHESTRATOR_ENABLED=true`，allowlist 2 个 hash；
  36min 内 messages orchestrator 21 success，failover 0。
- **新进展**：`/v1/responses` 首次出现 orchestrator 样本（36min 内 40 success），
  §5 覆盖面改善；同 endpoint legacy 仍有 42 success + 1 error（500×1），可作协对照。
- 早期信号（样本小，仅记录）：responses orchestrator P95 56s vs legacy 35.6s（+57%），
  若窗口后期仍超 20% 回归线需按 §5 处理；`/v1/chat/completions` orchestrator 恢复命中（1 个）。
- 候选新起点 2026-08-26 11:17 CST，预计新终点 2026-09-02 11:17 CST；
  legacy 对照（messages 流式）自 8-25 起持续为零，§1 条件 4 待解。

### 2026-08-27 日检结论、归因与已执行回滚

- 固定求值时点：2026-08-27 11:55:18 CST；Prometheus 3.6.0，查询区间严格为此前
  24 小时。`increase` 的小数来自抓取边界外推，表中请求数按最接近整数留证。
- 部署连续性：relay-gateway 仍运行 image `e82ab380`，启动于 2026-08-26 11:16:25 CST，
  `RestartCount=0`；`RELAY_ORCHESTRATOR_ENABLED=true`，allowlist 摘要数量为 2，未记录原始 hash。
- `/v1/responses` orchestrator 的 502 出现在以 15:16、16:16、17:16、18:16 CST 为
  求值点的连续四个 `[1h]` 桶，分别约 2、2、34、14 个；同期 legacy 仍有成功样本。
  18:16 CST 后 orchestrator 样本归零，不能把后续 502 为零解释成恢复。
- quota 生命周期闭合：responses `reserve≈154 = commit≈87 + release≈66 + 差额≈1`；
  messages 流式与非流式也各只剩约 1 个抓取边界 / 在飞差额。`quota_error=0`，failover
  switched / exhausted 均为 0，未见 reservation 卡单。
- billing / usage attribution 只读交叉核对：固定窗口内两边均为 1,797 个已结算请求；
  `stream=false/channel=143`、`stream=true/channel=846`、
  `stream=true/subscription=808` 完全一致。billing endpoint 进一步分解为 messages
  `channel 143(false)+759(true)、subscription 582(true)`，responses
  `channel 87(true)、subscription 226(true)`；stream、channel / subscription account 归因未见不一致。
- 判定：responses orchestrator 成功率比 legacy 低 37.86 个百分点，5xx 高 37.15 个百分点，
  P95 回归约 449%，同时 `/v1/chat/completions` 在日检窗口没有样本，当前窗口不满足 §5
  通过条件。
- 原始请求明细在用户关注的约 147 次口径中进一步分解：94 次成功由 `test` API-key 渠道
  `Kimi-K3` 贡献 87 次、订阅账号 4 的 `glm-5.3` 贡献 7 次；53 次 502 中，42 次选择
  `StepFun` / `step-explore`，11 次选择 `test` / `Kimi-K3`。Prometheus 固定窗口因抓取边界
  外推显示约 140 次、87 次成功，两种口径不可直接混加。
- `StepFun` 42 次失败已确认是本地协议兼容缺口，不是 StepFun 上游故障：orchestrator 把
  `/v1/responses` 标为 `responses` 入站格式，但 Anthropic API-key adaptor 当时只接受原生
  `anthropic_messages`，在发起 HTTP 请求前即返回“不支持 responses”；这些请求多数在
  25–70ms 内结束，随后被映射为 502。
- `test` / `Kimi-K3` 的 11 次 502 多在 156–299s 后结束，形态符合上游或网络超时；当时
  没有留存可安全展示的上游响应体，不能进一步断言具体供应商错误。该问题与 StepFun 的
  本地即时转换失败分开处理。
- Claude 通过线上 `/v1/messages` 调用 `step-explore` 时出现的 `Error editing file` 属于另一条
  Messages / 工具调用链路；当前证据不能证明与 Responses adaptor 缺口同源，需以后凭该请求的
  时间点或 request ID 核对模型输出和工具执行结果。
- 12:45:56 CST 已备份生产 compose `.env`，设置 `RELAY_ORCHESTRATOR_ENABLED=false` 并只重建
  relay-gateway；新容器于 12:46:02 CST 启动，`RestartCount=0`。随后指标确认
  `/v1/messages` 与 `/v1/responses` 均产生 `execution_path="legacy"` 的新终态样本，回滚生效。
- `test` 渠道暂不直接禁用：路由核对显示 `Kimi-K3` 等多个模型只有该渠道的精确生产能力；
  Kimi 订阅账号仅发布 `k3`、`k3-256k` 等别名，没有可无损替换的现成路由。修复恢复时继续
  使用既有 2-token allowlist 控制暴露面，不在本次事故处理中冒险改写模型映射。

### 中断记录 3：2026-08-27 回滚、修复部署与 canary 恢复

- 12:46 CST 按 §5 关闭 orchestrator 后确认两个主入口均重新产生 legacy 终态样本；此前从
  2026-08-26 11:17 CST 开始的观察窗口终止。
- 修复内容：Anthropic API-key adaptor 新增 Responses→Anthropic Messages 请求转换、Anthropic
  非流式响应→Responses 转换和 SSE 双向终态转换；继续移除第三方 Messages 服务不普遍支持的
  `thinking`、`output_config` 和 server-tool `type` 扩展。请求、非流式、流式以及 orchestrator
  forwarder 到 `/v1/messages` 的回归测试均已补齐。
- 部署前验证：`go test ./internal/adaptor ./internal/server` 通过；不依赖外部进程的服务端完整
  测试集通过。`go test ./...` 仅有 `test/e2e/suite` 因本机未启动 3000 / 9001 / 9004 服务失败，
  其余包全部通过。
- 15:39:40 CST 部署 amd64 镜像 `178eae0c3cd9`，旧镜像保留为
  `docker-compose-relay-gateway:rollback-20260827-153929`；先保持 flag=false 完成启动检查。
  15:40:24 CST 以原 2-token allowlist 恢复 flag=true，容器 `RestartCount=0`。
- canary 初始结果：前 2 分钟 `/v1/messages` orchestrator 产生 5 个 success / 200，均包含真实
  `StepFun` / `step-explore` 成功请求；尚无新的 `/v1/responses` orchestrator 终态样本，因此这只能
  证明部署与 Messages 路径正常，不能把 15:40 CST 直接记作 §1 的正式新起点。须等待 Responses
  真实样本并确认 StepFun 不再出现 25–70ms 的本地转换 502。

## 5. 通过与回滚

7 天窗口通过需同时满足：

- 每个实际承载 allowlisted 生产流量的 endpoint / stream 均有 orchestrator 样本，不能用零样本判定通过；
- orchestrator 成功率不低于同 endpoint / stream 的 legacy 对照，且没有新增稳定错误类型；
- orchestrator P95 相对 legacy 不回归超过 20%；
- `quota_error=0`，没有重复 Commit、成功流 Release、截断流 Commit 或 reservation 卡单；
- failover exhausted 比例没有显著高于 legacy，reason 分布能够解释；
- billing 的 endpoint / stream 归因正确，usage 日志的 stream、channel / subscription account
  归因一致。

出现稳定性错误、计费不一致、P95 超线或协议兼容回归时，清空 allowlist 或关闭
`RELAY_ORCHESTRATOR_ENABLED`，所有请求立即回到 legacy。回滚不删除旧 handler，也不需要数据库
回滚；恢复观察时重新确认 §1 的起点。
