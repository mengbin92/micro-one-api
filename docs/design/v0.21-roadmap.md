# micro-one-api v0.21 阶段路线图

> 制定日期：2026-08-14  
> 代码基线：`develop@8231710`（v0.20.0 发布提交）  
> 最近发布：[v0.20.2](../releases/release-v0.20.2.md)（2026-08-14，v0.21 阶段中的 PATCH 补强）\
> 版本节奏：本文是「执行阶段入口」，不是已完成的软件版本；阶段目标未达成前，兼容性补丁继续以 `v0.20.x` PATCH 发布，阶段完成后才形成 `v0.21.0` 候选。\
> **状态：进行中，当前唯一执行入口**。`docs/TODO.md` 与 `docs/README.md` 的路线图入口均以本文为准。  
> 定位：触发式观察 + 稳定性沉淀。不主动扩张产品范围；P3 议题维持准入评估，禁止跳过触发条件直接立项。

## 0. 执行状态总览

| 优先级 | 主题 | 状态 | 说明 |
|--------|------|------|------|
| P0 | v0.20 迁移后首个结算周期对账 | ✅ 已完成（2026-08-14） | 首个完整结算周期于 v0.20.0 上线当日（2026-08-14）执行对账，验收全部通过；记录见 [v0.21-p0-reconciliation-record.md](./v0.21-p0-reconciliation-record.md) |
| P0 | 文档事实源收口 | ✅ 本路线图建立 | v0.19 执行记录已归档，TODO/README 入口已同步 |
| P1 | MySQL/Postgres migration smoke | ✅ 已完成（2026-08-14） | CI `Migration smoke (mysql)` / `(postgres)` 均通过；覆盖 fresh、repeat、状态审计与失败注入 |
| P1 | release 挂钩 admin e2e | 🔄 稳定性计数中 | 最终 export 竞态修复后 main nightly 已连续 3 次双 suite 成功（含 2026-08-16 scheduled run `31922896563` 与 `d5b0024` 手动 run `31924463747`）；准入标准为连续 5 次或稳定 1–2 周。记录见 [v0.21-nightly-e2e-stability.md](./v0.21-nightly-e2e-stability.md) |
| P2 | P3-0 指标季度观察基线 | 🔄 首次快照已建立 | 可复用模板见 [p3-quarterly-baseline-template.md](../observability/p3-quarterly-baseline-template.md)；首次快照见 [p3-baseline-2026Q3.md](../observability/p3-baseline-2026Q3.md)。Prometheus 保留窗口约 41h、Grafana 只读凭据缺失，dedupe 冲突 counter 缺口均已登记 |
| P2 | 手动分区 DDL 预演 | ⬜ 触发式 | 达到 1GB / 1000 万行阈值前演练；必须先应用迁移 `078`；手动脚本前置守卫已补齐（见 §4.2） |
| P2 | P2.3 热点文件拆分 | ⬜ 触发式 | 仅在对应文件因业务需求再次修改时启动 |
| P3 | 五大议题 | ⬜ 触发式 | 排队 / 会话窗口 / 分区 / log-service 合并 / grpc-gateway，准入条件不变 |

## 1. 背景与现状

v0.19 已完成兼容性契约矩阵、迁移静态治理、CI 测试分层、基础设施单测补强、工具链版本固定与开发体验收口；v0.19.1 随 PATCH 发布。v0.20.0 进一步交付：

- relay HTTP 入口请求级指标（P3-0），P3 触发条件进入可观测状态；
- 分区账本幂等加固：迁移 `078` 新增 `billing_ledger_dedupe_claims`，全局幂等闸门从 `billing_ledgers.ledger_dedupe_key` 唯一索引迁移到 claim 表主键原子仲裁；
- 管理后台表格筛选竞态修复与 nightly compose E2E / Playwright smoke 修复。

v0.21 阶段执行期间已继续发布两个兼容性 PATCH：[v0.20.1](../releases/release-v0.20.1.md)（前端依赖安全修复）与 [v0.20.2](../releases/release-v0.20.2.md)（上游成本管理 + claim 覆盖守护）。这不表示阶段切换回 v0.20：`v0.21` 是当前阶段计划名，`v0.20.x` 是从 v0.20 基线延续的补丁版本序列；本文完成定义未满足前，不发布 `v0.21.0`。

当前遗留集中在两处：真实 MySQL/Postgres 方言的迁移执行验证尚未自动化；release 流程尚未挂钩 e2e。公开文档入口此前仍指向 v0.17，本文建立后已完成收口。

## 2. P0 — 事件驱动的资金安全验证

### 2.1 v0.20 迁移后首个结算周期对账（事件驱动，不随版本顺延）

**触发条件**：v0.20.0 上线后的第一个完整结算周期结束。

**原因**：迁移 `078` 改变了资金幂等闸门语义——`billing_ledgers` 上的全局唯一索引在分区方案下被移除，幂等由非分区 `billing_ledger_dedupe_claims` 表的主键在同一事务内仲裁。该语义必须有真实结算数据验证，不能只依赖单测。

**验收**：

- 结算周期内无未解释的重复 `ledger_dedupe_key` 账本；✅（0 重复；19 条迁移窗口孤儿已归因，唯一索引兜底）
- claim 冲突统一映射 `ErrLedgerDedupeExists` → `ErrDuplicateRequest`（HTTP 409），无驱动错误泄漏；✅（代码 + 单测链路验证）
- 对账脚本输出通过，异常项有闭环记录；✅（billing 全量对账 account 3 / channel 5 / receivable 1 为历史口径遗留；DB 侧检查全 PASS，毛利 24h +720,759 / 7d +7,362,712）
- 复核对账 / 告警阈值是否需要随新幂等语义调整。✅（无需调整；建议新增 claim 覆盖完整性检查入 DB 侧）

### 2.2 文档事实源收口（✅ 2026-08-14）

- v0.19 执行记录归档为 [v0.19-v0.20-execution-record.md](./v0.19-v0.20-execution-record.md)，状态补至 v0.20.0 发布；
- 本文成为唯一当前执行入口；
- `docs/TODO.md` 顶部入口与 `docs/README.md` 快速入口 / 路线图列表同步指向本文。

## 3. P1 — 质量门禁补齐

### 3.1 MySQL/Postgres migration smoke（可拆 PATCH）

- 目标：在 CI 中用 MySQL / PostgreSQL service-container 真实执行 fresh + incremental 迁移，补齐 SQLite 已自动化之外的两方言运行时验证；
- 现状：`make migration-check` 覆盖三方言静态镜像、ownership 与重复前缀，但不执行 SQL；Postgres / SQLite README 中“CI 已运行 scratch database”的表述超前于实际 CI，需随本项一并修正；
- 实施切分：第一阶段先落地 **fresh + repeat 幂等 + 状态审计**；incremental 先覆盖 consolidated baseline 之后的最近增量镜像，不强求补齐全部历史升级链路；
- 验收：CI 失败注入能捕获方言语法 / 约束问题，迁移日志与 `schema_migrations` 状态可审计；锁等待场景待后续真实 DDL 需要时再扩展。

### 3.2 release 挂钩 admin e2e（先准入，后提升）

- 前置条件：nightly compose E2E + Playwright admin smoke 在 v0.20.0 修复后连续稳定（建议连续 5 次或稳定 1–2 周）；
- 当前阻塞：2026-08-14 main nightly（run `31768353982`）compose E2E 与 Playwright 双失败；同日 develop 手动触发（run `31766504932`）双成功。需下载失败工件，先区分代码回归、环境/外部依赖波动与 flaky，再建立逐日稳定性记录；
- 验收：nightly 达到稳定标准后，release tag 工作流在镜像发布前执行 e2e，失败即阻断发布；失败工件上传保留，并至少经历一次真实发布验证。

## 4. P2 — 观察基线与触发式沉淀

### 4.1 P3-0 指标季度观察基线

- 已建立可复用季度模板：[P3-0 季度观察基线模板](../observability/p3-quarterly-baseline-template.md)，覆盖入口 RPS、P50/P95/P99、429/502、熔断、dedupe claim 行数增长率与双向覆盖检查；
- 2026-08-15 已完成首次观察快照 [p3-baseline-2026Q3.md](../observability/p3-baseline-2026Q3.md)：claim/ledger 双向覆盖与重复 key 检查均 PASS，429/502 未满足 P3 触发条件；Prometheus 仅保留约 41h 数据且 Grafana 只读凭据缺失，报告已登记后续数据质量缺口；
- 当前 dedupe claim 冲突 / 回滚没有应用级 Prometheus counter；为避免仅为观察而改动资金热路径，首季使用 DB 行数增长与 claim ↔ ledger 双向覆盖作为代理口径，并在模板中登记指标缺口；
- 输出进入后续 P3 议题准入评估，不直接触发实现。

### 4.2 手动分区 DDL 预演（触发式）

- 阈值：生产 ledger / logs 表接近 1GB 或 1000 万行前，完成一次生产等价演练；
- **硬性顺序**：ledger 分区脚本执行前必须已应用迁移 `078`，且 `billing_ledger_dedupe_claims` 存在并完成存量回填；
- ✅（2026-08-15）`migrations/manual/phase3_*.sql` 已内置前置守卫：ledger 脚本会校验 `schema_migrations`、迁移 `078` 已应用且 ledger → claim 覆盖为 0 缺失；logs 脚本会校验目标 schema 已纳入 `schema_migrations` 治理。任一条件不满足即 `SIGNAL` 中止，后续 DDL 不会执行。守卫已用 MySQL 8.0 scratch 容器验证通过/阻断路径；
- 验收：演练记录包含备份、执行时长、锁影响、回滚方案与分区校验结果。

### 4.3 热点文件拆分（触发式）

- 触发条件不变：admin http / channel data / billing biz / relay biz 仅在对应文件因业务需求再次修改时拆分；
- 禁止为了拆分而拆分，禁止同一 PR 混拆多个热点。

## 5. P3 — 准入评估议题（不排期实施）

| 议题 | 触发信号 | 立项前要求 |
|------|----------|------------|
| 负载感知排队 | 502/429 持续阈值、账号池饱和、熔断频次 | 独立 ADR + 容量数据 |
| 会话窗口统一 | Claude 多账号池 / 窗口冲突真实出现 | 独立 ADR + 现网案例 |
| 表分区 | 表规模接近 1GB / 1000 万行 | 先完成 §4.2 预演与 `078` 对账 |
| log-service 合并 | 成本 / 运维复杂度证据 | 独立 ADR + 成本对比 |
| grpc-gateway 迁移 | 外部 REST facade 需求变化 | 独立 ADR + 契约影响面 |

### 4.4 下阶段执行顺序（2026-08-14）

1. P1 migration smoke：脚本 + Make target + CI service-container + README 漂移修正；
2. 同步诊断 main nightly 失败并建立稳定性记录，不先修改 `release.yml`；
3. migration smoke 稳定后评估 incremental 覆盖扩展；
4. 并行建立 P3-0 季度观察基线；
5. nightly 达到准入标准后，才把 admin e2e 挂入 release 工作流；
6. 手动分区脚本可提前补前置守卫，但生产预演仍等待 1GB / 1000 万行阈值。

## 6. 质量门槛

普通 PR：

```bash
make test-unit
make test-race
./scripts/check-architecture.sh
make migration-check
cd web && npm run lint && npm test -- --run && npm run build
```

迁移 / 资金 PR 追加：

```bash
go test -race ./app/billing/... ./app/admin/...
# SQLite fresh + upgrade smoke
# MySQL/PostgreSQL migration smoke（本阶段补齐后必须执行）
```

release 候选追加：

```bash
make test-e2e-suite
cd web && npx playwright test
./scripts/check-deployment-docs.sh
```

## 7. 完成定义

- v0.20 后首个结算周期对账有记录、有结论，风险闭环；
- MySQL/Postgres migration smoke 在 CI 中稳定运行；
- nightly e2e 达到稳定标准后，release 工作流接入并至少经历一次真实发布验证；
- P3-0 季度观察基线形成可复用报告，P3 五议题维持触发式；
- 未以未触发的产品功能或大规模重构作为 v0.21 发布前置。
