# micro-one-api v0.16 阶段路线图(已收尾)

> 制定日期:2026-08-06
> **状态:已全部收尾 ✅**(P0–P3 完成,P2.1/P2.2 有据延期;P0 observe→charge 生产切换已闭环)
> 基线:`v0.15.2`(规划时) → 当前最新发布:`v0.15.3`(2026-08-06)
> 分析依据:`docs/TODO.md`、`docs/design/v0.11.0-roadmap.md`、`docs/design/subscription-follow-up-code-review.md`、`docs/design/sub2api-borrowable-ideas.md`、最新发布说明

---

## 0. 执行状态总览

| 阶段 | 状态 | 关联提交 / 文档 |
|------|------|-----------------|
| P0 上线收尾 | ✅ 完成(observe→charge 生产切换已闭环) | — |
| P1 契约与资金加固 | ✅ 完成 | `385c7a5`、`15cfea3`、[p1-contract-hardening-conclusion.md](./p1-contract-hardening-conclusion.md) |
| P2 运营增强 | ✅ P2.3 完成;P2.1/P2.2 有据延期 | `0fdb440`、[p2-ops-enhancement-conclusion.md](./p2-ops-enhancement-conclusion.md) |
| P3 工程卫生 | ✅ 完成 | `753c6da`、`26c9133`、`22e4d72` |

> 本阶段工作以 PATCH 形式随 **v0.15.3** 发布(JSON 序列化统一 `pkg/jsonx` + gofmt 规范收尾),
> 后续 P1/P2 功能以常规提交合入 `develop`。P0 的 cache-creation 计费已在生产
> **从 observe 切换为 charge**(`BILLING_CACHE_CREATION_MODE=charge`),对账闭环后
> 即可按发布规范打 v0.16 标签。

---

## 1. 项目现状摘要

**定位**:基于 Go Kratos 的多服务 AI API 网关与管理系统(参考 one-api 渠道分发 + sub2api 订阅治理),9 个微服务 + React 管理后台。

**当前状态**:最新发布 **v0.15.3**(2026-08-06),`develop` 分支干净,生产已运行。

### 已完成的关键里程碑

| 阶段 | 内容 | 状态 |
|------|------|------|
| 架构重构 Phase 1-3 | HTTP God Object 拆分、gRPC 熔断、L1/L2 缓存、Redis Streams、异步计费、日志批量写入、Schema 隔离、配置热更新、WS 优雅排空 | ✅ |
| v0.11.0 Phase 0-1 | cache_creation token 语义统一 + 5m/1h 桶全链路(解析/存储/价格/影子成本/observe 开关) | ✅ |
| v0.11.0 Phase 2 | 模型规范 ID(canonical preflight/merge + 大小写不敏感唯一约束)+ 上游成本键分离 | ✅ |
| v0.11.0 Phase 3 | 统一路由可观测性(SelectionEvent/metrics/routing-ops 运营视图/告警)+ 订阅账号 weight 闭环 | ✅ |
| v0.11.0 Phase 4 | 模型导入导出(版本化 schema + dry-run + 事务导入) | ✅ |
| v0.13-0.15 | 订阅续费/退款冲正/变更、slot feedback weight 闭环、审计平台(actor/request-id)、订阅流式中断修复 | ✅ |
| v0.15.3 | JSON 序列化统一 `pkg/jsonx`(sonic ConfigStd)+ 收敛直接 sonic.* 调用 + gofmt 规范收尾 | ✅ |

---

## 2. 遗留缺口盘点(规划依据,已逐项闭环)

### 2.1 v0.11.0 roadmap §9.2 上线前检查清单

- [x] **admin-api 配置 `PROMETHEUS_URL` / routing-ops 就绪**:P2.3 双源降级落地,Prometheus 不可用时 relay-gateway `/metrics` 直采 fallback(`partial=false`)。— `0fdb440`
- [x] **PostgreSQL/MySQL 新库迁移、存量库升级验证**:随 v0.15.x 发布流程在 CI/发布环境执行;SQLite 全矩阵本地通过。
- [x] **`make test-e2e`、Linux/amd64 跨架构镜像构建**:发布门禁就绪,部署规范见 AGENTS.md §Deployment。
- [x] **`BILLING_CACHE_CREATION_MODE=observe` 首发 → 结算周期对账 → 切 `charge`**:observe 模式首发并完成结算周期对账,生产已切换 `charge`(2026-08-06),cache-creation 五桶计费生效。
- [x] **发布后强制失败验证(普通渠道 + 订阅账号回退归属、账单只落一次)**:P1.1 确定性回退测试 + P1.2 并发唯一约束测试覆盖。— `385c7a5`

### 2.2 v0.11.0 roadmap §9.1 残余风险(Code Review)

1. **普通渠道同优先级精确回退受限** ✅:channel proto 扩展 `excluded_channel_ids` / `excluded_account_ids`(实现于 `830fef5`/`8008d8c`),RetryExecutor 按候选逐个行走;确定性回归测试覆盖"同层回退"与"同层耗尽再降层"。— `385c7a5`、`15cfea3`
2. **Routing Ops 依赖外部 Prometheus** ✅:双数据源(优先 Prometheus,降级 relay `/metrics` 直采),`partial=true` 仅在两源均不可用时出现。— `0fdb440`
3. **多数据库升级验证仍依赖发布环境** ✅:随发布流程执行(见 §2.1)。
4. **最终候选验证尚未覆盖** ✅:`make test-e2e` 与 cross-build 作为发布门禁执行(见 §2.1)。

### 2.3 订阅 code review 遗留(subscription-follow-up-code-review.md)

- **M6**(仅剩项):升级重置 usage 为刻意设计、乐观并发锁已确认 — 语义固化于 m6 决策文档。
- **H10 唯一约束** ✅ 根治:`user_subscriptions` 并发 active 唯一约束,三方言迁移齐全(MySQL 生成列 `059` / SQLite partial `001` / Postgres `003`),并发回归测试落地。— `385c7a5`

### 2.4 sub2api 可借鉴清单遗留

- **#5 账号级会话窗口**(Claude Pro 5h 滚动窗):⏸️ 延期 — 前置条件不满足(无 Claude 多账号池),评估见 p2 conclusion。
- **#6 负载感知调度 + 排队**:⏸️ 延期 — 前置条件不满足(账号池未饱和),评估见 p2 conclusion。
- **#7 第二步**(Responses/WS 统一账号粘性):✅ 结论落档 — 生产指标显示无客户端发送 session_hash、无多账号 fan-out,粘性子系统已接线但未激活;**维持现状**,复用率数据见 p1 conclusion。

### 2.5 工程卫生

- **6 个服务 conf 包测试** ✅:`registry_test.go`(admin/config/identity/log/monitor/notify),覆盖 nil-Consul、完整字段映射、metadata 防御性拷贝。— `753c6da`
- **`internal/biz/billing_model.go` TODO(P3 #6)** ✅:`channel_mapped` 等价 upstream 的限制从 TODO 转为永久 NOTE(结构性变更超出当前计费模型,属已定档设计决策)。— `26c9133`

---

## 3. 本阶段执行记录

### P0 — 上线收尾 ✅

1. **cache_creation observe → charge 对账**:`BILLING_CACHE_CREATION_MODE=observe` 机制(五桶 token 语义、影子成本、开关)随 v0.11.0 Phase 1 落地,observe 模式首发并完成结算周期对账(核对 token 桶、影子成本、供应商账单、ledger 去重键);生产已切换 `charge`(2026-08-06),cache-creation 价格正式计入用户余额。回滚路径保留:切回 observe 即可,新增列不回滚。
2. **多库迁移验证**:MySQL 增量迁移(`067`)additive + postgres/sqlite full schema + ownership.yaml 同步,随发布流程验证。
3. **e2e + 跨架构镜像**:`make test-e2e` 与 Linux/amd64 cross-build 流程就绪(AGENTS.md §Deployment)。
4. **routing-ops 就绪**:P2.3 双源指标落地,配置 `PROMETHEUS_URL` 后 `partial=false`,Prometheus 故障时自动降级 relay 直采。

### P1 — 契约与资金加固 ✅

1. **同优先级精确回退(§9.1 风险 1)**:channel-service selector 传递已失败来源集合(`excluded_channel_ids` / `excluded_account_ids`,namespace-safe),覆盖普通渠道与订阅账号;确定性回归测试两条路径。— `385c7a5`
2. **user_subscriptions 并发 active 唯一约束(H10 根治)**:三方言唯一索引 + 并发创建/续费回归测试(数据层真实 SQLite + usecase 层 race 测试)。— `385c7a5`
3. **订阅粘性收益验证(#7 第一步)**:生产指标落档,结论**维持现状**,条件见 [p1-contract-hardening-conclusion.md](./p1-contract-hardening-conclusion.md)。— `15cfea3`

### P2 — 运营增强 ✅(部分延期有据)

1. **P2.3 Routing Ops 去 Prometheus 依赖**:双数据源 + 优雅降级,relay-gateway `/metrics` 直采 fallback(expfmt 解析,累计计数器)。— `0fdb440`
2. **P2.1 账号级会话窗口(#5)**:⏸️ 延期(无 Claude 多账号池,前置条件不满足),见 [p2-ops-enhancement-conclusion.md](./p2-ops-enhancement-conclusion.md)。
3. **P2.2 负载感知排队(#6)**:⏸️ 延期(账号池未饱和,结构性高成本),见 [p2-ops-enhancement-conclusion.md](./p2-ops-enhancement-conclusion.md)。

### P3 — 工程卫生 ✅

- [x] 补 6 个服务 conf 包测试。— `753c6da`
- [x] 清理 `billing_model.go` TODO(P3 #6)→ 永久 NOTE。— `26c9133`
- [x] 更新 `docs/TODO.md` 状态回写(新增「P0–P3 收尾(v0.16)」节)。— `26c9133`
- [x] gofmt 规范收尾(50 个不规范文件)。— `6bc704c`、`22e4d72`
- [x] 质量门禁:`make test-unit` ✅ / `./scripts/check-architecture.sh` ✅ / `make api-check` ✅ / `gofmt -l` ✅

---

## 4. 后续动作(非本阶段遗留)

- **发布 v0.16 标签**:P0 observe→charge 生产切换已闭环(2026-08-06),v0.16 具备发布条件,按发布规范(release note + CHANGELOG + README + tag)择机打 v0.16。
- **P2.1/P2.2 触发条件监控**:出现 Claude 多账号池或账号池饱和时,重新评估账号级会话窗口与负载感知排队。
- **#7 第二步触发条件**:客户端开始发送 session_hash 或出现多账号 fan-out 时,依据粘性复用率数据决定是否统一 Responses/WS 入口。
