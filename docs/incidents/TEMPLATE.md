# 事故复盘：<简短标题>

> 事故编号：`INC-YYYYMMDD-NN`
> 状态：调查中 / 已缓解 / 已关闭
> 发现时间：`YYYY-MM-DD HH:MM:SS TZ`
> 影响开始：`YYYY-MM-DD HH:MM:SS TZ`，无法恢复时写“未知”并说明原因
> 影响结束：`YYYY-MM-DD HH:MM:SS TZ`，仍在持续时写“持续中”
> 记录提交 / 镜像：`<git sha>` / `<image digest>`

## 摘要

用两到三句话说明用户看到的现象、受影响能力和当前状态。没有证据的历史事实写“未知”，不要用估计值替代观测。

## 影响范围

- 受影响服务、接口、模型或数据路径：
- 受影响环境与区域：
- 受影响用户数 / 请求数：
- 错误率、延迟或账务影响：
- 未受影响的相邻能力及证据：

每个数字注明查询窗口、数据源和查询语句或面板链接。无法恢复的样本明确标记 `UNKNOWN`。

## 发现与时间线

| 时间（含时区） | 事件 | 证据 |
| --- | --- | --- |
|  | 首个已知影响 |  |
|  | 首次发现 / 告警 |  |
|  | 缓解动作 |  |
|  | 恢复确认 |  |

记录“影响开始 → 发现”的耗时和“发现 → 恢复”的耗时。若起点未知，保留未知并说明缺少的日志或指标。

## 根因与促成因素

区分直接根因、放大影响的促成因素和未被证据支持的假设。引用对应代码、配置、日志、请求 ID 或指标窗口。

## 响应与恢复

按实际顺序记录止损、修复、部署和回滚准备。说明每个动作改变了什么，以及如何确认没有扩大账务、权限或数据风险。

## 验证

- 故障可重现的最小用例：
- 修复前失败证据：
- 修复后本地 / 隔离 / 生产证据：
- 回归命令和结果：
- 剩余边界：

## 后续行动

| 优先级 | 行动 | Owner | 截止日期 | 完成证据 |
| --- | --- | --- | --- | --- |
|  |  |  |  |  |

## 附件

只保存脱敏证据。不得写入 API Key、访问令牌、支付凭据、完整请求正文或上游响应正文。
