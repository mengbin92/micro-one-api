# Go 工具链固定版本（v0.19 P2.1）
#
# 这是本地与 CI 共用的唯一版本源：
#   - 本地：Makefile `include scripts/tool-versions.env`（make init / security-* 目标）
#   - CI：  .github/workflows/security.yml `source scripts/tool-versions.env`
#
# 规则：
#   - 禁止在任何 go install 中直接使用 @latest —— 版本必须先写入本文件。
#   - 升级方式：一次性 bump 本文件的多个版本，作为独立 PR 提交（可借助
#     `make tools-upgrade-check` 查看固定版本与最新版的差异），不要混入功能改动。
#   - 若启用 Dependabot/Renovate 自动化，先限定频率（如每月一次）并按工具分组，
#     且只修改本文件，不允许自动化直接改写 Makefile 或 workflow。
#
# 查询最新版本：go list -m -f '{{.Version}}' <module>@latest
BUF_VERSION=v1.72.0
WIRE_VERSION=v0.7.0
GOSEC_VERSION=v2.28.0
GOVULNCHECK_VERSION=v1.6.0
GITLEAKS_VERSION=v8.30.1
SYFT_VERSION=v1.51.0
GO_LICENSES_VERSION=v1.6.0
